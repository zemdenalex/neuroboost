# Вопросы Денису (очередь, 25.09 22:00)

Уже заданы в сессии и ждут его ответа в терминале/документах — НЕ задавать повторно, а спросить статус:
- push локальных коммитов на staging (он просил сначала документ `docs/relizy/plan-v0.4.11.6-i-v0.4.12.md`)
- «да» на PR #10 (после его раздела 3 — подзадачи)

Новые:
1. Финансирование: все три выбранные программы закрыты для ИП сейчас. Регистрировать ООО (открывает Сколково, ФРИИ/Сбер500 в следующих волнах) — или искать то, что берёт ИП/физлицо? (варианты: ООО (рекомендую обсудить с корнем) · только ИП-программы · пауза)
2. Сирота графа `decision-graph-now-enabled` (битая ссылка на `memory-split-claude-graph-remember`) — удалить узел или связать?

## Go 1.22 → 1.26 в образах и CI (25.09, отчёт `docs/team/research/V003-20260925-res-deps-security.md`)
🟢 Готово на ветке `chore/go-1.26` (`c65c476`, не запушена): Go 1.26 + Alpine 3.22 в обоих Dockerfile и CI, оба образа собрались локально; образ API проверен запуском 26.09: чистый Postgres 16 → миграции прошли, `/api/health` = ok; zoneinfo в образе есть (`Europe/Moscow`, `Asia/Tokyo`), Alpine 3.22.6. Бот-образ только собран. Прод и CI сейчас собираются на `golang:1.22-alpine` — без поддержки, 8+ достижимых уязвимостей stdlib. Код уже гоняется на 1.26.3 локально.
- Поднять вместе со следующим релизом (Recommended) — `~20 мин · ~40k`
- Отдельным выкатом сейчас — `~30 мин · ~50k`, пересборка обоих серверов
- Не трогать пока

## Задачи дня: viewer берёт общую задачу, которую не может закрыть (аудит 25.09, M6)
Viewer общего календаря может взять на день общую задачу, но закрыть её не может → его день не станет 🟩, и причины он не увидит. Viewer-приглашения уже живые (`/calendars/{id}/invites` с ролью `viewer`), так что это не совсем латентно.
- Не предлагать и не давать брать задачи из календарей «только чтение» (Recommended) — `~20 мин · ~40k`
- Давать брать; закрыл владелец → засчитать viewer'у (как `doneOnDay`) — `~30 мин · ~60k`
- Оставить как есть
