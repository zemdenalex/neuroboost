# Подзадачи в боте — задачи

> Источник: план Дениса 23.09, день 2 (`docs/plan-3-dnya-zhizn-v-neuroboost-2026-09-23.md:36`):
> *«на карточке задачи список подзадач с ✅, кнопка «➕ Подзадача»; задача с подзадачами = проект»*.
> Его слова 23.09: *«Deploy and review, improve what we did then subtasks»*. Объём — ровно эта строка,
> всё сверх неё — Ruling в этом файле на его проверку.
> 🔴 `api-go` ночью не трогать. Чего API не умеет — записать сюда как «ждёт Дениса».

## Что уже выяснено (24.09, ночь)

- API: `parent_id` есть в create/list/get (`api-go/internal/tasks/types.go:44,96,146`), веб строит дерево (Alt+→).
- 🔴 **Календарь подзадачи.** `POST /api/tasks` берёт `calendar_id` из запроса или **личный календарь**
  (`handlers.go` `calendars.WritableIDFor(ctx, userID, req.CalendarID)`). От родителя **не наследует**.
  Бот обязан послать `calendar_id` родителя, иначе подзадача задачи из общего «Дом» уйдёт в личный
  календарь автора и Настя её не увидит. Тест: родитель в общем календаре.
- В бот-типе `api.Task` (`bot/internal/api/client.go:84`) нет `ParentID` и `CalendarID` — добавить.
- ⚠ `callback_data` ≤ 64 байт: id задачи 36 знаков, два id не влезают. ✅ по подзадаче несёт только
  её id; родителя найти по `parent_id` в только что прочитанном списке.

## Шаги

- [x] 0. ✅ отдаёт: фильтра по status нет. Прочитать `listTasks` (`api-go/internal/tasks/handlers.go:399`): отдаёт ли `GET /api/tasks` без status задачи DONE? Если нет — сделанные подзадачи пропадут с карточки и счёт «1 из 3» врёт → ждёт Дениса (правка API)
- [x] 1. `api.Task` + `ParentID`, `CalendarID`; `CreateTask` умеет `parent_id` и `calendar_id`
- [x] 2. Карточка (`handleTaskAction`, `bot/internal/handlers/tasks.go:98`): под полями строка «📁 Подзадачи: 1 из 3» и кнопки подзадач (⬜/✅ + название); у подзадачи — кнопка «↑ К задаче»
- [x] 3. ✅ подзадачи — через **существующий** путь `handleTaskDone` / `taskRepeats` (серия закрывается на день, не навсегда — дефект 20.09); после — перерисовать карточку родителя
- [x] 4. «➕ Подзадача» → свой текстовый flow (как `dayTaskDateFlow` в `flows.go`), иначе quick-save съест строку; экран с ℹ️ (help_scan), текст без ⚙️ и без длинных тире
- [x] 5. Проверить порядок `HasPrefix` в `handler.go` для нового префикса
- [x] 6. Списки (📋, Сегодня, предложение задач дня) **не прятать** подзадачи — Ruling: иначе подзадача со сроком сегодня исчезнет из «Сегодня». Метка родителя в списке — вкус Дениса, не делать
- [x] 7. Тесты + сабботажи; оба модуля; push/deploy dev — **только если Денис не проходит чеклист D3 в этот момент**
- [x] 8. Чеклист `docs/proverka-bota-…-podzadachi.md`, открыть в Obsidian

## Итог (24.09, утро)

`6891e72`: 5 тестов (красные до кода), 4 сабботажа красные, оба модуля зелёные. Dev-бот на `6891e72`. Чеклист: `docs/proverka-bota-2026-09-24-podzadachi.md`. Найдено по дороге: `st_` занят статистикой → префикс `sb_`.
