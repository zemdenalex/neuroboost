# Задачи по проходу 3 (v0.4.11.4) — 23.09.2026

**Источник:** `ref/feedback/bot-proverka-v04114-prohod3-otvet-denisa-2026-09-23.md` — чеклист A–F
с отметками Дениса. **Итог прохода:** A1–A18, B1–B17, D1–D9, E1–E5, F1–F6 — ✅; E6–E8 — не для dev.
Регрессий нет. Ниже — его замечания, по одному на строку, с тем, что нашлось в коде.

## Починить (дефекты и прямые просьбы)

- [x] **P1. «✏️ Изменить» после создания ведёт в список событий, а не в созданное.** (под A1)
  ✅ сверено 26.09: `74c4e69` · Денис ✅ A1 в `docs/proverka-bota-2026-09-23-pravki-prohoda3.md`; выпущено в v0.4.11.5
      Путь — черновик события: `handlers/draftflow.go` `createFromDraft` отвечает
      `keyboards.AgendaActions` (там `event_pick` = список). Путь «📅 В календарь» уже отвечает
      `EventCard(ev.ID)` — правильно. Чинить: `createOne` возвращает id, ответ — `EventCard(id)`.
- [x] **P2. Пробел между числом и индикатором в клетке календаря** («20 ▄», не «20▄»). (под A18, D9)
  ✅ сверено 26.09: `5a5ccb7` · Денис ✅ A2 в `docs/proverka-bota-2026-09-23-pravki-prohoda3.md`; выпущено в v0.4.11.5
- [x] **P3. Шкалу статистики менять и из календаря** — «а то непонятно». (D9) Кнопка 📏 на месячной
  ✅ сверено 26.09: `c844777` · Денис ✅ A3 в `docs/proverka-bota-2026-09-23-pravki-prohoda3.md`; выпущено в v0.4.11.5
      сетке → тот же экран `handleScalePick`, «назад» — в календарь.
- [x] **P4. «завтра с 01:00 до 22:00 работать» не понял диапазон:** начало 01:00, конец потерян,
  ✅ сверено 26.09: `b72931f` · Денис ✅ A4 в `docs/proverka-bota-2026-09-23-pravki-prohoda3.md`; выпущено в v0.4.11.5
      название «с до 22:00 работать». То же для «завтра работать с 01:00 до 22:00». (под B17)
      Разбор строки: `bot/internal/parse`.
- [x] **P5. F7 решено: объяснение ℹ️ заменяет экран, «« Назад» возвращает экран.** Сейчас —
  ✅ сверено 26.09: `d139c16` · Денис ✅ A6 в `docs/proverka-bota-2026-09-23-pravki-prohoda3.md`; выпущено в v0.4.11.5
      отдельное сообщение (`handlers/help.go`). Нужно помнить экран (текст с разметкой и кнопки)
      до замены — Telegram отдаёт их в `cb.Message` (`Text` + `Entities` + `ReplyMarkup`).
- [x] **P6. E7: у Дениса `/broadcast` на dev — «недоступна».** Он админ на проде; на dev база —
  ✅ сверено 26.09: Денис ✅ A7 (пробный прогон `/broadcast` на dev) в `docs/proverka-bota-2026-09-23-pravki-prohoda3.md`; выпущено в v0.4.11.5
      копия прода. Проверить, по какому признаку `IsAdmin` и почему dev-аккаунт его не имеет.

## Статус на 23.09, 09:10 (сделано локально, не запушено, ждёт твоей проверки на dev)

- P1 → `74c4e69` · P2 → `5a5ccb7` (пробел) · P3 → `c844777` · P4 → `b72931f` · P5 → `d139c16`
  (галочки ставишь ты после проверки на dev).
- 🔴 **P6 — нашлось больше, чем «нет админа»: копия прод → dev не работала никогда.** Шаг CI
  (`.github/workflows/ci.yml:307–312`) пишет в базу `neuroboost` внутри dev-контейнера, а dev живёт в
  `neuroboost_dev` (`POSTGRES_DB`). psql падает на каждом push, ошибка уходит в `/dev/null`, шаг печатает
  «DB sync failed, continuing with existing data» и CI зелёный. Проверено 23.09 только чтением:
  в dev 808 пользователей (копятся e2e-аккаунты), в проде 8; админов в dev 0, в проде 1 (с Telegram и
  почтой). Отсюда: ⚠ в чеклистах «push залил в dev копию прода, твои задачи пропали» — **неверно**;
  рассылка на проде сработает (админ есть), на dev — нет.
- ⚠ Починить копию «в лоб» опасно: она сотрёт dev-базу, включая e2e-аккаунт, на котором держится CI
  (`e2e` job). Решение — твоё.

## Спроектировать (новая работа, нужен разговор)

- [ ] **N1. Онбординг объясняет всё главное меню** — задачи, события, заметки, настройки и т.д.,
      «или хотя бы кнопку что это»; `ℹ️` на остальных шагах онбординга.
- [ ] **N2. Веб:** перенести всё новое из бота в веб, починить веб для мобильных, мини-апп в боте.
      Его слова: «надо будет скоро думать в сторону веб версии».

## Вне этого прохода, но в очереди (23.09)

- D2 «задачи дня» в боте — план `docs/superpowers/plans/2026-09-23-day-tasks-d2-bot.md` (готов).
- Строка = задача сразу — собрано локально (`c23bf67`), на dev ещё нет.
