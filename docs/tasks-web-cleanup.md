# Задачи: уборка веба (Admin, Tools, Profile, feedback)

> Откуда: аудит `docs/team/research/V003-20260924-res-admin-tools-audit.md` (опус, только чтение кода; две
> главные находки я проверил сам: feedback на публичном роуте, `Profile.tsx:31` с `xp: 1250`).
> Решения Дениса 25.09 (ночь): Profile — *«real numbers, remove mock data… simple gamification is easier»*;
> feedback — привязать пользователя; Admin — все четыре пункта уборки.
> Всё на `develop` локально, без push, пока он проходит `docs/proverka-2026-09-24-bot-i-veb.md`.
> Каждый фикс по TDD и с сабботажем; проверка `pnpm typecheck && pnpm lint && pnpm test --run && pnpm build`,
> для `api-go` и `bot` — `go build ./... && go vet ./... && go test ./...`.

## 1. Admin: убрать

- [x] 1.1 Кнопка Import Features, `generateImportItems` (129 строк seed'а с IP прод-сервера), `importing/importDone` (`Admin.tsx:94`, `:144-159`, `:365-372`, `:903-1093`) · ~10 мин · ~15k
- [x] 1.2 Вкладка Users-заглушка (`Admin.tsx:228-234`, `:419-424`) · ~5 мин · ~10k
- [x] 1.3 Проверить, что IP `62.76.228.106` больше нигде в `web/` нет (`search.py`) · ~2 мин
- [x] 1.4 `POST /api/feedback/import` после 1.1 никем не вызывается: удалить роут и handler с тестом · ~10 мин · ~15k

## 2. Feedback с отправителем

- [x] 2.1 api-go: optional-JWT на `POST /api/feedback` (токен есть → `user_id`, нет → аноним, как сейчас). Тест через HTTP: с токеном строка с `user_id`, без токена без него, битый токен = аноним, а не 401 · ~20 мин · ~40k
- [x] 2.2 web: `api/feedback.ts:56` слать токен · ~5 мин
- [x] 2.3 бот: уже шлёт токен (`bot/internal/api/client.go:426`), проверить тестом · ~5 мин
- [x] 2.4 `source` = `web` / `bot` по белому списку вместо захардкоженного `user` · ~10 мин · ~15k

## 3. Profile: только настоящие числа

- [x] 3.1 Убрать мок (`Profile.tsx:29-49`: XP, уровень, стрик, 42 задачи, бейджи) · ~10 мин
- [x] 3.2 Настоящие счётчики: задачи `DONE`, дни задач дня взяты / полные, стрик взятых дней, рефлексии. Откуда брать — по существующим API, без миграции · ~30 мин · ~80k
- [x] 3.3 Простая геймификация (Денис 25.09, утро: «Day tasks drive it»: +10 XP за сделанную задачу, +25 за полный день задач дня, +5 за рефлексию, уровень каждые 250 XP; стрик рядом) (его слова: *«simple gamification is easier»*): XP и уровень **из настоящих действий**, без новых таблиц. Сначала показать Денису формулу вариантами (что даёт XP, сколько на уровень), потом строить

## 4. Admin и Tools: доделать частичное

- [x] 4.1 (`lib/admin/feedbackStats.ts`, отдельная загрузка без фильтров; e2e с подменой API) A2 Overview считает по фильтру Backlog → отдельная загрузка без фильтров · ~15 мин · ~20k
- [x] 4.2 («Not saved» у строки; e2e + сабботаж) A4 ошибки правки строки молчат → статус «не сохранено» · ~15 мин · ~20k
- [ ] 🟡 4.3 (отложено 25.09: версия через `-ldflags` требует build-arg в `docker compose build` на серверах, то есть правки деплоя; не делать посреди его прохода) A6 Health: версия `0.4.0` захардкожена (и gotcha 8) → одна `Version` через `-ldflags`; миграция `v0` при ошибке → «unknown» · ~30 мин · ~50k
- [x] 4.4 A7 Logs забиваются собственным опросом → не логировать `GET /api/admin/logs`; DEBUG в stdout при info → `Enabled(level)` · ~20 мин · ~30k
- [x] 4.5 T0a/T0b мёртвая ветка «Скоро» и `tools-context.txt` · ~5 мин
- [x] 4.6 T0c флаг `tools` не учтён в `VerticalSidebar`, `FabBottomSheet` и роуте · ~10 мин · ~15k
- [x] 4.7 T2 Kanban: пустая колонка «Входящие», drop в «Запланировано» без события, повторяющаяся задача закрывает всю серию (и на странице Tasks), хардкод «Add», молчаливый откат · ~50 мин · ~80k
- [x] 4.8 T1a Pomodoro: привязка к read-only задаче теряет блок → сначала воспроизвести · ~20 мин · ~30k
  ✅ 25.09: воспроизведено по коду (viewer-роль реальна: приглашения с `viewer` живые; `log-time` пишет по `WritableIDsFor` → 404 → `tracking.ts` откатывает блок). Список привязки теперь `lib/pomodoro/linkableTasks` — только задачи в календарях, где можно писать (`writableCalendars`). Тест + саботаж красный. ⚠ Живьём двумя аккаунтами не проверено
- [x] 4.9 (`taskQuadrant`/`dropPriority` в `lib/tools/eisenhower.ts`, 3 сабботажа; сообщение, если задача легла не в тот квадрант из-за срока) (Денис 25.09: срочность из `due_date`, ≤ 2 дней или просрочено; важность = priority 1–2; перетаскивание меняет только priority) Eisenhower строит обе оси из одного `priority` (T3) — заменить срочность на `due_date` / переименовать в «Доску приоритетов» / убрать
- [x] 4.10 (`lib/tools/timeBudget.ts`, разбивка в аккаунте `time_budget`, старая копия из браузера подхватывается один раз, неделя убрана; e2e + сабботажи) (Денис 25.09: «Бюджет времени» из рабочих часов, хранить в аккаунте, убрать декоративную неделю) TimeBlocking — калькулятор в `localStorage`, «неделя» декоративная (T4) — переименовать в «Бюджет времени» и взять рабочие часы / убрать

- [x] 4.11 Страница **Tasks**: галочка у повторяющейся задачи закрывает всю серию (`pages/Tasks/Tasks.tsx:217` пишет `status`). Честный фикс больше Kanban'а: список не знает о вхождениях, значит, нужен статус «сегодня сделано» по `/api/tasks/occurrences`, галочка → `/occurrences`, и отмена так же · ~40 мин · ~90k
  ✅ 25.09: `lib/tasks/tickAction` — галочка у идущей серии отвечает за день (`POST /occurrences`), повторная снимает ответ; отмена в тосте — новое `state: "open"` в API (удаляет строку дня). Пакетное «закрыть» — так же. Тесты: `occurrence_open_test.go` (HTTP+БД, саботаж красный), `tickAction.test.ts`, e2e `task-tick-series.spec.ts` (красный на старом поведении). ⚠ Отмена работает на staging только после push: API там ещё без `open`

## 5. Потом (ROADMAP, не здесь)

Управление пользователями, сброс пароля, уведомление админу о новом feedback, rate limit на публичном `POST /api/feedback`, `RealIP`.
