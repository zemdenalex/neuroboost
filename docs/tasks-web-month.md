# Задачи: месячный вид в вебе (5 вариантов)

> Спека: `docs/team/architecture/V003-20260924-arc-web-month-view.md`.
> Варианты: https://claude.ai/artifact/MZTR5gNNjhR5ekL3LhD4QN.
> Решения агента R1–R9 записаны в `E:/Projects/graph/decisions.jsonl` как `ratify`.
> Каждый шаг идёт по TDD: тест красный, потом зелёный, потом сабботаж (тест снова краснеет, файл
> восстановлен побайтно). Проверка перед коммитом: `pnpm typecheck && pnpm test --run && pnpm build && pnpm lint`.
> 🔴 Всё в ветке `feat/web-month`. Не пушить в `develop`, пока идёт проход Дениса и нет релиза v0.4.11.6 (R9).

## Каркас + вариант A: можно выпускать как вид по умолчанию

- [x] 0. `git switch -c feat/web-month develop` · ~1 мин
- [x] 1. `web/src/lib/calendar/monthGrid.ts` — `monthGrid(year, month) → string[42]` (YYYY-MM-DD, с понедельника) + `shiftMonth`. Тесты: месяц с 1-м числом в понедельник, февраль невисокосного года, декабрь→январь · ~10 мин · ~25k
- [x] 2. `web/src/lib/calendar/monthCells.ts` — `eventsByDay(events, days, tz)`: многодневное в каждом своём дне (R5), целодневные первыми, потом по времени; `cellRows(items, max) → {shown, more}`. Тесты: событие через полночь в зоне пользователя, многодневное, пустой день · ~15 мин · ~40k
- [x] 3. `web/src/lib/calendar/monthVariant.ts` — `readMonthVariant(settings)` (`month_view_variant`, неизвестное = `list`, R2); `readCalendarView()` / `saveCalendarView()` в `localStorage` `nb-calendar-view` с try/catch; `effectiveView(saved, isMobile)` (на телефоне всегда неделя/день). Тесты: все пять ключей, мусор, сбой storage, телефон · ~10 мин · ~25k
- [x] 4. Данные месяца: один `getEvents` на 42 дня; вынести один `listDays` так, чтобы и квадраты (`useDayColours`), и вариант E брали один `Day[]` (`lib/dayTasks/loadDayColours.ts` → `useDays` + производные цвета). Тест: `loadDayColours` не изменил поведение (старая таблица зелёная) · ~15 мин · ~40k
- [x] 5. `components/MonthView/MonthView.tsx` — шапка (◀ ▶, «Сегодня», месяц словами), сетка 6×7, фильтр скрытых календарей, клетка A (`cells/ListCell.tsx`); сегодня выделено; квадрат дня. Заменить заглушки в `MonthView/` (удалить `monthview-context.txt` и пустые файлы) · ~25 мин · ~80k
- [x] 6. `pages/Calendar/Calendar.tsx` — переключатель Неделя / Месяц (скрыт < 768 px), запоминание (шаг 3). Клик по дню → неделя с этим днём (смещение недели от сегодня); одиночный клик ждёт 250 мс (R6); двойной → `EventEditor` на 1 час с `work_start` (R7). Логику клика вынести в `lib/calendar/monthClick.ts` с тестом на таймер (vi.useFakeTimers) · ~25 мин · ~70k
- [x] 7. Перетаскивание плашки (A) на другой день: pointer-drag с `DRAG_THRESHOLD_PX` (R8). Чистая функция `shiftByDays(startISO, endISO, days, tz)` в `lib/calendar/` — тест через переход DST (Europe/Berlin, последнее воскресенье октября): 10:00 остаётся 10:00. Повторяющееся → `withScope` (как в неделе) · ~30 мин · ~90k
- [x] 8. i18n ru/en (`calendar.json`: month, week, today, more, day tasks off) + тест паритета, как `daytasks` · ~5 мин · ~10k
- [x] 9. e2e `web/e2e/month-view.spec.ts`: переключатель запоминается после reload; клик → неделя; двойной клик → редактор; перетаскивание события на тестовом аккаунте → утверждение по API, событие возвращено назад. Прогнать `web/scripts/e2e-local.sh --staging` до push: должен упасть (месяца на staging нет) · ~25 мин · ~80k
- [x] 10. Сабботажи по шагам 1–7 (по одному на функцию), итог в конце этого файла · ~10 мин · ~20k

## Варианты B–E и выбор в настройках

- [x] 11. `pages/Settings/sections/MonthViewSection.tsx` — пять вариантов с мини-превью (статичные квадраты, без данных), сохранение через `autoSave` → `month_view_variant`; месяц перерисовывается сразу · ~15 мин · ~40k
- [x] 12. B `cells/ClassicCell.tsx`: 2 залитые плашки, «ещё N», лёгкая заливка цветом дня; перетаскивание как в A · ~10 мин · ~30k
- [x] 13. C `cells/HeatCell.tsx` + `lib/calendar/busyShare.ts` (R4: минуты / 960, cap 1, без целодневных; тесты: пустой день, 20 ч событий → 1, событие через полночь делится по дням); `title` с названиями · ~15 мин · ~40k
- [x] 14. D `SplitMonth.tsx`: компактный месяц с точками (до 4, цвет календаря) + список выбранного дня; выбор дня кликом, двойной клик как везде · ~20 мин · ~60k
- [x] 15. E `cells/CommitCell.tsx`: задачи дня из общего `Day[]` (шаг 4), `done/target`, полоса, тонировка по `level`; будущие дни — взятые задачи; задачи дня выключены → строка и ссылка в ⚙️ · ~15 мин · ~45k
- [x] 16. e2e: смена варианта в ⚙️ меняет месяц (по одному утверждению на вариант), восстановить `list` в конце · ~15 мин · ~40k
- [x] 17. Свежий ревьюер (opus) по всей ветке; критичные и важные — фикс с тестом красный→зелёный · ~20 мин · ~60k
- [x] 18. Чек-лист Денису `docs/proverka-veba-<дата>-mesyac.md` по §4 спеки, открыть в Obsidian · ~10 мин · ~15k
- [ ] 19. После его прохода веба и релиза v0.4.11.6: merge `feat/web-month` → `develop`, CI зелёный с e2e

## Сабботажи (шаг 10)

Все красные, файл восстановлен побайтно: `monthGrid` (понедельник, год) · `weekOffset` · `eventsByDay` (зона, полночь, сортировка — после того как сабботаж сортировки остался зелёным и тест усилен) · `cellRows` · `readMonthVariant` · `effectiveView` · хранилище, которое бросает · `loadDays` (выключено, сбой) · `createDayClick` (двойной, cancel) · `shiftByDays` (DST осенью; весенний случай — второй проход, тест добавлен после зелёного сабботажа) · `localTimeOn` · e2e перетаскивания (сдвиг назад → красный). e2e месяца: 3 красных на staging до push, 3 зелёных локально.

## Ревью ветки (шаг 17, свежий ревьюер opus): 0 Critical · 5 Important · 13 Minor

Исправлено (тест красный → зелёный, сабботаж по каждому):
- I1 гонка ответов: старый ответ перекрашивал новый месяц → `lib/async/latestOnly.ts`
- I2 e2e двойного клика ждёт дольше 250 мс · I3 e2e проверяет, что открылась неделя ЭТОГО дня (`data-day` у заголовка колонки недели)
- I4 e2e перетаскивания: ожидание через `shiftByDays` в зоне аккаунта, месяц после сброса жив, сброс на свой день ничего не двигает
- I5 логика перетаскивания и маршрут клика вынесены в чистые `monthDrag.ts` / `routeCellClick` с тестами; тест `squareColour`
- M1 DST-тесты `busyShare` и `eventsByDay` (Берлин, 25 октября) · M3 E тонируется один раз · M4 e2e варианта E на подменённом дне + «выключены» · M5 заглушка PATCH отвечает реальным пользователем · M6 PATCH в тестах вариантов заблокирован · M7 `pointercancel` · M8 нажатие на плашку отменяет ждущий клик
- 🔴 **Найдено заодно, уже на staging с 24.09:** квадраты задач дня в **неделе** стояли на день раньше в любой зоне восточнее UTC (Москва): `dayKey` резал UTC-дату у локальной полуночи. Тест был зелёным, потому что подавал UTC-полночь, которой сетка не делает. Починено, тест на Москву и Лос-Анджелес

Отложено (Minor):
- M2 тест «в любой зоне процесса» гоняется только в одной зоне (CI в UTC)
- M9 тройной клик открывает и закрывает редактор (фон ловит третий клик)
- M10 месяц не следует за неделей, которую листал
- M11 клавиатура: дни не фокусируются; у переключателя неверная подпись
- M12 D: многодневное событие со временем в последующие дни подписано «весь день»
- ✅ M13 (Денис 24.09: «300, и настройка»): 250 мс ожидания короче системного двойного клика (Windows ~500 мс), медленный двойной клик уводит в неделю. Поднять до ~450 мс (одиночный клик медленнее) или оставить

## Найдено по дороге

- [x] Редактор события (`components/Calendar/EventEditor`) не закрывается по Escape: ни одного обработчика, кроме `TimeInput`. Правило проекта (`.claude/rules/react-frontend.md`): «Escape + клик мимо». Найдено e2e месяца 24.09; отдельный фикс с тестом · ~15 мин · ~30k. Не чинил сам: Escape выбросит набранное, и диалог «только это / все» тоже слушает Escape

- [x] `web/src/i18n/locales/{ru,en}/settings.json` — 10 длинных тире в текстах пользователю (не в `monthView`, тот чистый и под тестом). Правило Дениса — без длинных тире; поправить и распространить тест паритета на весь файл · ~10 мин · ~20k

- [x] Длинные тире в остальных текстах веба: `home` 9 · `onboarding` 24 · `reminders` 7 · `tasks` 2 · `tools` 6 (ru+en, 48 строк). Это переписывание копирайта (в русском тире часто грамматическое: «Календарь — это истина»), как в боте в D2; потом тест на все `locales/*.json` · ~30 мин · ~60k

- [x] Escape в редакторе (Денис 24.09: «1 + press escape again»): нетронутый закрывается, после ввода первый Escape предупреждает, второй закрывает; диалог «только это / все» свой Escape сохраняет (`EventEditor/escapeStep.ts`, e2e в `month-view.spec.ts`)
- [x] Ожидание второго клика 300 мс и ползунок 150–800 в ⚙️ → «Месячный вид» (`month_click_wait_ms`)
- [ ] Настройки: разделы и поиск (Денис 24.09: «divide settings into sections, add search, etc. but not now»)

## Промпт лупа

```
/loop Денис 24.09: «start building the web version, now it's far behind» · «let's build all of them, make a default and other to choose in settings». Чек-лист: docs/tasks-web-month.md, спека docs/team/architecture/V003-20260924-arc-web-month-view.md. Дальше — docs/agents/queue.md.
```
