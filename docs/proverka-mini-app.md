# Проверка: Telegram Mini App (dev-бот)

> Открывать **после** того, как я напишу «Mini App на staging» (нужны push `develop` и `WEBAPP_URL`
> у dev-бота на nl-2 — их делаю я после твоего прохода по `docs/proverka-2026-09-24-bot-i-veb.md`).
> Бот: **@NeuroBoost_dev_bot**. С телефона, в самом Telegram.
> Отмечай `- [x]`; что не так — строкой под пунктом, можно скриншот.

## 1. Вход без логина

- [ ] В чате с ботом слева от поля ввода кнопка **«Календарь»** (вместо «Меню»)
- [ ] Нажал → открылся календарь NeuroBoost внутри Telegram, **без экрана входа**
- [ ] Это твой аккаунт: твои события и задачи, имя в профиле твоё

## 2. Внутри Telegram

- [ ] Перетащил событие **вверх или вниз** — событие переехало, окно Mini App **не свернулось**
- [ ] Открыл ⚙️ Настройки (или Профиль) → вверху слева кнопка **«Назад» Telegram** → вернула на прошлый экран
- [ ] На вкладках снизу (Календарь, Повестка, Задачи) кнопки «Назад» **нет**
- [ ] В меню аватара **нет «Выйти»** (внутри Telegram выход бессмыслен: следующий запуск войдёт снова)

## 2b. Светлая тема (твой выбор B, 25.09)

- [ ] Telegram в **светлой** теме → Mini App открылся **светлым**: светлый фон, тёмный текст, шапка Telegram в цвет
- [ ] Переключил тему Telegram, не закрывая Mini App → приложение перекрасилось само
- [ ] В тёмном Telegram всё как раньше

## 3. На телефоне вообще (это же веб, правки 25.09)

- [ ] Календарь открывается на **текущем часе**, а не на 00:00
- [ ] Заголовок даты в одну строку («Fri, Sep 25» / «пт, 25 сент.»)
- [ ] Задачи: счётчики не рвутся посреди слова; слева от кружка нет пустого места
- [ ] Планирование: список задач не уходит под полосу «Загрузка»
- [ ] Профиль: аватар над именем, строка XP в одну-две строки

## 4. Ссылки на экран (нужен твой шаг в BotFather)

Ссылки вида `t.me/NeuroBoost_dev_bot/<app>?startapp=dt` работают, только если в @BotFather
заведено приложение: `/newapp` → выбрать dev-бота → название, описание, картинка 640×360 →
URL `https://dev.neuroboost.website` → короткое имя (например `app`). Скажи мне имя — проверю
ссылки сам и допишу пункты. Кнопке «Календарь» из п. 1 это не нужно.

- [ ] (после `/newapp`) `t.me/NeuroBoost_dev_bot/<имя>?startapp=dt` → открылись задачи дня

## Что осталось на потом

- День по ссылке (`d-2026-09-25`) — у календаря ещё нет параметра даты
- Прод-бот — только после релиза веба, по твоему «да»
