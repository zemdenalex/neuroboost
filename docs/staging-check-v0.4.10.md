# Staging-проход перед promotion v0.4.10

**Где:** https://dev.neuroboost.website · **Что промоутим:** 99 коммитов, `develop` → `main`
**Интерактивная версия:** https://claude.ai/code/artifact/b236b75a-33c3-42e0-b3eb-74f69c734adf
**PR:** https://github.com/zemdenalex/neuroboost/pull/9 · **Тег после мержа:** `v0.4.10`

Состав: v0.4.10 + P1 (быстрое создание задач) + P2 (напоминания, шаги 1–9 из 10).

## Перед стартом — три важных момента

0. 🔴 **Сами уведомления пока не приходят — и это не баг.** `SERVICE_TOKEN` ещё не заведён,
   бот пока на RU-хосте, где Telegram заблокирован. Проверяем **только UI смещений**.
1. **Telegram Login Widget на staging не работает.** Widget-бот зарегистрирован только на
   prod-origin `neuroboost.website`. Логинься **email + password**.
2. **База на staging синхронизирована с production** (deploy-dev делает `pg_dump` с prod).
   Данные твои настоящие, но **это отдельная база** — правки на staging в prod не попадут.

## Что уже проверено машиной (можно не тратить время)

Go build / vet / test (6 пакетов) · бот-модуль build / vet / test · `pnpm typecheck` ·
**267 тестов / 34 файла** · `pnpm build` — всё зелёное, CI на `develop` тоже.
Миграция `000010` прогнана вживую в обе стороны, дедупликация проверена реальным `duplicate key`.

Чистая логика (recurrence, timezone, pomodoro-движок, week-range, drag-математика) закрыта
тестами. **Проверять руками нужно то, что тестами не покрыто вообще: UI.**

## Приоритет проверки — по риску

### 🔴 1. P2 — UI напоминаний (никогда не открывался в браузере)

- [ ] Событие → «Расширенные» → блок «Напоминания»: список вместо старой выпадашки
- [ ] Ввести «30» + Enter — смещение добавилось, форма события **не** отправилась
- [ ] Форматы `2ч` / `3д` / `1н` читаются правильно; мусор даёт понятную ошибку
- [ ] Крестик убирает смещение; пустой список сохраняется как «без напоминаний»
- [ ] Пресеты: «важное» даёт 5 смещений, свой набор показывает «Свой»
- [ ] Сохранить событие, переоткрыть — смещения на месте
- [ ] Задача: quick-add → `Ctrl+E` ×2 → блок «Напоминания»
      ⚠ Без срока блок заблокирован — это правильно, считать не от чего
- [ ] Settings → «Напоминания»: пресеты, время сводки, тихие часы — автосохранение
- [ ] Сменить пресет по умолчанию для задач → новая задача сразу получает его смещения
- [ ] en ↔ ru — блок напоминаний и единицы времени переведены

### 🔴 2. Onboarding и contextual help (никогда не открывались в браузере)

Самая большая новая фича, покрыта только unit-тестами чистых функций. React-компонентного
harness в проекте нет.

- [ ] Первый вход: видна welcome-карточка с циклом capture → schedule → do → reflect
- [ ] Guided checklist: отмечается сам по мере создания task/event/reflection, исчезает по завершении
- [ ] Кнопка «?» на **каждой** странице (Calendar, Tasks, Planning, Reflections, Tools, Settings)
- [ ] «Показать подсказки» в Settings — все **3 стиля**: bubbles / walkthrough (spotlight) / markers
- [ ] Guided empty states: пустой Planning и пустой Dashboard
- [ ] Переключить язык **en ↔ ru** — подсказки и checklist переведены полностью
- [ ] Сбросить onboarding (localStorage) и пройти заново — состояние не залипает

### 🔴 3. Pomodoro (тоже только unit-тесты)

- [ ] Запустить таймер, **уйти на другую страницу** — плавающий виджет живёт и продолжает счёт
- [ ] Три стиля виджета: pill / card / bar
- [ ] Дождаться перехода фазы — звук + уведомление срабатывают **один раз**, не дублируются
- [ ] Цикл-pips отражают реальную позицию в цикле
- [ ] По завершении — toast с **Undo**; проверить, что Undo действительно откатывает
- [ ] Время пишется в задачу (`actual_minutes`) — видно в задаче после завершения
- [ ] **Перезагрузить страницу с активным таймером** — восстанавливается по timestamp, не сбрасывается

### 🟠 4. P1 — быстрое создание задач (локально проверял, на staging — нет)

- [ ] `/tasks` — курсор сразу в quick-add
- [ ] Задача → Enter → создалась, фокус остался; три подряд без мыши
- [ ] `Ctrl+E` разворачивает поля, Escape сворачивает
- [ ] `Alt+→` / `Alt+←` — дерево подзадач
- [ ] Вставка нескольких строк из буфера → создались все
- [ ] Закрытие в один клик + Undo; Shift+клик → массовые действия
- [ ] Задача с оценкой времени на календарь → событие нужной длины, не 60 минут

### 🟠 5. Регрессии в том, чем пользуешься каждый день

- [ ] Календарь: создать / подвинуть / растянуть событие (одиночное, в пределах дня)
- [ ] Перетащить задачу из sidebar на календарь → создаётся событие, статус SCHEDULED
- [ ] Recurring: событие с повтором **отображается** корректно
      ⚠️ **Редактировать/двигать recurring не пробуй — вернёт 500.** Это известный R1, чинится
      отдельно; delete одного вхождения уже починен, но фронт ещё не шлёт `scope`.
- [ ] Неделя листается вперёд/назад, «Today» возвращает на текущую
- [ ] Export → Import: выгрузить, загрузить обратно, данные не потерялись и не задвоились
- [ ] Мобильный размер (375px): шапка не наезжает, editor не переполняется, FAB не перекрывает feedback

### 🟡 6. Мелочи, которые чинились в этом релизе

- [ ] Due-date у задачи не съезжает на день при редактировании
- [ ] Даты и дни недели на выбранном языке (не хардкод ru/en)
- [ ] Profile: таймзона принимает любую IANA-зону
- [ ] Пересекающиеся события лежат рядом в колонках, а не друг на друге

## Известные баги — НЕ считать регрессией

| Что | Почему |
|---|---|
| Уведомление в Telegram не приходит | Нет `SERVICE_TOKEN` + бот на RU-хосте. Шаг 10 из P2 |
| Задача из `Ctrl+K` не появляется в списке без перезагрузки | Известное ограничение P1 |
| `Ctrl+K` не работает, пока курсор в поле ввода | Намеренно — иначе съедал бы набор |
| Два чекбокса в строке задачи | Нерешённый вопрос дизайна |
| Отдельного редактора задачи нет | `TaskEditor` — заглушка `TODO` |
| Multi-day event: resize схлопывает событие | **MD1/MD2**. Математика починена и покрыта тестами, `useWeekGridDrag` ещё не подключён |
| Mobile: нет 3-day / agenda / mini-month | **MV1**, отложено с v0.4.5 |
| Console: warning про passive listener | **PE1**, косметика |

## 🆕 R1 — повторяющиеся события (собрано 28.07, на staging ЕЩЁ НЕТ)

Это **не** часть v0.4.10 и на staging пока не выкатано — я не пушил, пока ты ходишь по
чеклисту. Прожать после того, как закончишь с остальным (скажи — запушу).

Раньше любая правка повтора возвращала 500 и календарь откатывался. Теперь спрашивается
«Только это событие / Все повторы».

| # | Что сделать | Ожидаемо |
|---|---|---|
| R1-1 | Создать событие с повтором «каждый день» | Видно в нескольких днях недели |
| R1-2 | Перетащить **один** повтор мышью → «Только это событие» | Переехал только он, остальные на месте |
| R1-3 | Перетащить повтор → «Все повторы» | Сдвинулись **все**, включая прошлые дни, на ту же величину |
| R1-4 | Открыть повтор, сменить заголовок, Save → «Только это событие» | Заголовок сменился у одного, дубля рядом нет |
| R1-5 | Удалить один повтор → «Только это событие» | Исчез один, серия жива |
| R1-6 | Удалить повтор → «Все повторы» | Исчезла вся серия |
| R1-7 | Отметить «Запомнить выбор», затем ещё раз подвинуть повтор | Диалог **больше не появляется** |
| R1-8 | Settings → «Повторяющиеся события» → «Спрашивать каждый раз» | Диалог вернулся |
| R1-9 | Нажать «Отмена» в диалоге | Событие вернулось на место, ничего не сохранилось |
| R1-10 | Обычное (не повторяющееся) событие подвинуть | Диалога **нет**, работает как раньше |

⚠️ На R1-9 событие на долю секунды останется там, куда его бросили, и вернётся — это
известное ограничение (у сетки нет оптимистичного состояния), а не баг. Если мигание
заметное и раздражает — скажи, вынесу отдельным пунктом.

## Если всё ок

Скажи — смёржу PR #9 и поставлю тег `v0.4.10`.
⚠️ Production деплоится **на push в `main`**, а не на тег: мерж = деплой.

## Если что-то сломано

Скажи что именно — заведу в ROADMAP и починю **до** promotion. Смысл staging-прохода ровно в
этом: v0.4.10 лежит непромоученным два месяца, и сегодняшний Sunday-инцидент — цена этой задержки.
Но выкатывать непроверенный UI поверх работающего prod — хуже.
