# Задачи: мобильный веб (375px)

> Денис 25.09: *«work in a loop to create telegram miniapp and fix mobile web»*.
> Откуда список: скриншот-тур `web/e2e/mobile-tour.spec.ts` (14 экранов, 375×667, сборка `--preview`
> против staging API, аккаунт Дениса, английский интерфейс e2e). Прогон:
> `NB_TOUR=1 NB_TOUR_DIR=<папка> web/scripts/e2e-local.sh --preview --project mobile e2e/mobile-tour.spec.ts`.
> 🔴 Только дефекты (перекрытие, раздавленный текст, пустое место, мёртвые настройки). Вкус (палитра,
> раскладка «как лучше») — вариантами Денису, не самому (`E:/Projects/.claude/rules/design-bans.md`).
> Mini App откроет этот же веб в Telegram, так что всё ниже чинится и для него.
> Правило проверки: до/после скриншот тем же туром, плюс `mobile-overflow.spec.ts` зелёный.

## Дефекты с тура 25.09

- [x] MW1 (`a5d96b0`) **Planning**: плашка Capacity (`sticky`) висит посреди экрана поверх списка «Unscheduled» (скрин `planning.png`) · ~15 мин · ~30k
- [x] MW2 (`e9d7586`) **Profile**: аватар 96px рядом с текстом → строка XP раздавлена на 4 строки, «Level1» слиплось; на мобиле аватар над текстом или меньше · ~10 мин · ~20k
- [x] MW3 (`f57d7e9`) **Tasks**: счётчики переносятся посреди слова («To / Do:», «Done / today:»); у быстрого ввода двойная рамка фокуса (рамка input внутри рамки контейнера) · ~15 мин · ~30k
- [ ] MW4 (🟢 заголовок даты коротким форматом на мобиле; 🟡 подсказка в две строки — вкус, вопрос Денису: убрать под «?» или оставить; 🟡 пустая полоса all-day 80px — `ALL_DAY_HEIGHT` в геометрии drag/resize в 5 местах, отдельный пункт с e2e перетаскивания) **Calendar**: шапка съедает треть экрана: подсказка «Tap: select • …» в две строки, пустая полоса all-day ~80px без событий, заголовок даты в две строки · ~25 мин · ~60k
- [x] MW5 (`66911c5`: не мёртвая настройка, а дефект — на телефоне «боковая панель» убирала верхнюю панель целиком, и это синхронизировалось с аккаунта; теперь телефон всегда с верхней, раздел только на десктопе) **Settings**: раздел Layout (верхняя панель / боковая) на мобиле ничего не меняет — там нижняя навигация; проверить и скрыть на мобиле · ~10 мин · ~20k
- [x] MW6 (Settings, Tools, Tasks: `p-4 sm:p-6`; остальные страницы уже были `p-4`) **Все страницы**: двойной отступ (страница `p-6` + карточка `p-6`) съедает 96px из 375; на мобиле `p-4` · ~20 мин · ~40k
- [x] MW7 (`9b20f62`: отступ под гамбургер только при `mobile_nav = hamburger`) **Шапка**: «NeuroBoost» сдвинут вправо от края без видимой причины (пустой слот слева?) · ~10 мин
- [ ] 🟡 MW8 (не дефект: обычный перенос фильтров; вкус — Денису) **Kanban**: фильтры приоритетов в три строки, шестерёнка отдельной строкой · ~10 мин
- [x] MW9 Второй тур 25.09 (после MW1–MW7): Planning, Profile, Tasks, шапка — исправлены на экранах. Новое — ниже
- [x] MW10 **Tasks**: невидимый (opacity-0 до hover) чекбокс выбора в каждой строке на телефоне — занимал ширину и отмечался тапом рядом с кружком; скрыт на телефоне, пока ничего не выбрано
- [x] MW11 (день открывается на часе «сейчас − 1», другая неделя на 08:00; `lib/calendar/initialScroll.ts`; 6 drag-спек писались под `scrollTop = 0` и упали — им добавлен `e2e/fixtures/grid.ts` `scrollGridToTop`; ⚠ `pnpm typecheck` не видит `web/e2e/` — ошибка `page is not defined` нашлась только прогоном) **Calendar**: день открывается на 00:00, и подпись «00:00» срезана шапкой дня; открывать на рабочем начале или текущем часе · ~20 мин · ~40k
- [ ] MW12 **Tasks**: название задачи переносится («позвонить в / банк») — три иконки справа всегда видны на телефоне (~110px); вариант: действия за «⋯» или свайпом — вкус, Денису

## Решения Дениса 25.09 (страница вариантов https://claude.ai/artifact/GqNKUBaqERKbD6vFzJRyBD)

- [x] MW13 (`lib/calendar/calendarChrome.ts`; высота полосы одна на всю геометрию, в drag-хук через ref; e2e 28/28 + 2 сабботажа; весь e2e 85 зелёных, красные только Mini App до push) Календарь, вариант **A**: подсказка на телефоне только первые 3 открытия (дальше под «?»); пустая полоса «весь день» на телефоне 20px (80px, когда в ней событие). Высота полосы течёт в геометрию drag (`ALL_DAY_HEIGHT` в 5 местах) → прогнать все drag-спеки · ~35 мин · ~90k
- [x] MW14 (`components/TaskRow/TaskRowActions.tsx`, ⚙️ «Действия с задачей на телефоне», ключ `task_row_actions`; 3 e2e + 2 сабботажа; весь e2e 88 зелёных, красные только Mini App до push) Строка задачи: *«all three, customizable in settings»* — «⋯»-меню, свайп влево, карточка по тапу; выбор в ⚙️ (по умолчанию «⋯», Ruling: рекомендованный вариант) · ~2 ч · ~250k
- [ ] MW15 Светлая тема по теме Telegram (его выбор B) — отдельный разбор `docs/tasks-light-theme.md`

## Найдено попутно

- [ ] 🟡 `e2e/month-view.spec.ts` «choosing a variant in Settings saves month_view_variant» флакает: `apiResponse.json: Response has been disposed` (1 из 3 прогонов 25.09, потом 2/2 зелёные) — гонка в собственном `route` спеки
- [x] (Денис 25.09: «yes, add it» → `@types/node@22` dev, `tsconfig.e2e.json`, `typecheck` проверяет и e2e; сабботаж `page` ловится) `pnpm typecheck` не проверял `web/e2e/` (ошибка `page is not defined` нашлась только прогоном) — отдельный `tsconfig` для e2e или `tsc -p e2e` в CI · ~15 мин

## Не входит

- Месячный вид на мобиле (решение 24.09: только десктоп, `effectiveView`)
- Смена палитры, шрифта, иконок
