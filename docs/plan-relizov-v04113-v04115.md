# План релизов v0.4.11.3 → v0.4.11.5

**Дата:** 18.09.2026 · **Решение Дениса 18.09 01:5x:** три релиза вместо одного.
**Первоисточник:** разговор 18.09 (быстрое добавление уже выпущено в v0.4.11.2; здесь — то,
что Настя и Денис нашли после).

## Почему режем именно так

Набралось **14 пунктов**. Спека связки аккаунтов одна оценивалась в «сессия с лишним на API +
полсессии веб + полсессии бот» — вместе получалось втрое больше, а документ на четырнадцать
пунктов не реализуют, его перечитывают.

🔴 **Граница не вкусовая, а деплойная.** Шесть пунктов не трогают `api-go` ни одним файлом.
Значит: нет миграции, нет мержа в `main`, нет бэкапа, нет прод-гейта — выкат руками на nl-2,
ровно как v0.4.11.1 и v0.4.11.2 (gotcha 19). Денис проверяет их на следующий день, а не после
прод-релиза.

| Релиз | Что | Чем выкатывается |
|---|---|---|
| **v0.4.11.3** — бот | карточки со всеми полями · календари в боте · приглашения ссылкой и по email · кнопки snooze под напоминанием · «сообщить об ошибке / предложить улучшение» · заметки о релизе | руками на nl-2, мержа нет |
| **v0.4.11.4** — API | повторяющиеся задачи · task↔event · частота напоминаний (долбёжка) · английский дайджест · язык кнопок под уведомлением · редактирование одного вхождения · API отвергает RRULE, который не разбирает | миграция + бэкап + мерж = **прод-релиз** |
| **v0.4.11.5** — связка | email ↔ Telegram, слияние аккаунтов | миграция + бэкап + мерж, **необратимо** |

## Состав по пунктам

| # | Пункт | Релиз | Откуда |
|---|---|---|---|
| 1 | Карточка называет **все** характеристики, пустые — словом «нет» | 11.3 | Денис, 18.09 |
| 2 | Календари в боте: создать · переименовать · цвет · пригласить · участники · выйти · удалить | 11.3 | Денис, 18.09 |
| 3 | Приглашение ссылкой **и** по email | 11.3 | Денис, 18.09 |
| 4 | Кнопки «отложить на 10 минут / на час» под напоминанием | 11.3 | Денис, 18.09 |
| 5 | Кнопка «сообщить об ошибке» и «предложить улучшение» | 11.3 | Денис, 17.09 |
| 6 | Заметки о релизе в боте | 11.3 | Денис, 17.09 |
| 7 | Повторяющиеся задачи с состоянием по дням и откладыванием | 11.4 | Настя через Дениса, 18.09 |
| 8 | Превратить задачу в событие и обратно / связать | 11.4 | Настя через Дениса, 17–18.09 |
| 9 | Частота напоминаний («каждые 10 минут, пока не ответил») — **и у задач, и у событий** | 11.4 | Денис, 18.09 |
| 10 | Утренний дайджест по-английски у всех (`reminders/digest.go:46,59,65`) | 11.4 | Настя через Дениса, 17.09 |
| 11 | Язык кнопок под уведомлением | 11.4 | v0.4.11.2, перенесено |
| 12 | Редактирование одного вхождения повтора | 11.4 | v0.4.11.2, перенесено |
| 13 | API отвергает RRULE, который не разбирает | 11.4 | v0.4.11.2, перенесено |
| 14 | Связка email ↔ Telegram и слияние аккаунтов | 11.5 | Денис, 17.09 |

## Спеки

- 11.3 — `docs/superpowers/specs/2026-09-18-bot-v04113-cards-calendars-design.md`
- 11.4 — `docs/superpowers/specs/2026-09-18-api-v04114-recurring-tasks-design.md`
- 11.5 — `docs/superpowers/specs/2026-09-17-account-linking-design.md`

## Что проверено в коде 18.09, а не предположено

| Утверждение | Свидетельство |
|---|---|
| У задач нет повтора вовсе — ни `rrule`, ни якоря | `000001_baseline.up.sql:68-84`, колонок нет |
| Связи задачи и события нет ни одной | `grep event_id` по всем миграциям — только `reminder` и `event_exception` |
| API календарей полон для всех пяти действий бота | `handlers.go:20` (`name`+`color`), `members.go:284` (`leaving := actorID == targetID`), `members.go:304` (владельцу выйти нельзя) |
| Пригласить можно **только по email** | `members_handlers.go:55` — без email 400. У Telegram-аккаунта email `NULL` ⇒ пригласить Настю этим путём нельзя, нужна ссылка |
| Snooze с произвольными минутами уже есть | `reminders/action.go:26-32` — default 10, потолок сутки |
| Напоминания по вхождениям уже ключуются верно | `000010_add_reminders.up.sql:33` — в ключе есть `task_id` и `occurrence_start` |

## Записано на потом (Денис, 18.09, по ходу проверки)

Это не в 11.3–11.5. Записано, чтобы не потерялось.

| # | Что | Его слова | Размер |
|---|---|---|---|
| A | 🔴 **Заметки как настоящая сущность, а не задача.** Сейчас «📝 Заметка» создаёт задачу. Нужен `note` со своим хранением, и **экспорт в `.md`** — поштучно и массово | «мы же сто процентов должны были создать или иметь уже такую сущность как note, у нас изначально нейробуст должен был с obsidian интегрироваться, поэтому .md должны были заметки быть» | миграция + API + веб + бот, отдельный релиз |
| B | **Обмен с Obsidian в обе стороны**: весь календарь и заметки выгрузить в `.md`, и наоборот — занести из `.md` | «в будущем будет задача чтобы всё можно было в .md экспортировать для obsidian и наоборот» | большой, своя спека |
| C | **День в календаре → «Изменить»**: показать события и задачи этого дня и дальше как обычное редактирование события, с пагинацией, если их много | «нужно еще сделать при нажатии на день, чтобы была кнопка изменить и там открываются именно события и задачи на этот день» | полсессии, чисто ботовая |

⚠ Пункт A объясняет, почему в карточке поле называется **«Описание», а не «Заметка»**: заметка —
это то, чем NeuroBoost собирался быть, и занимать это слово текстовым полем события значит
пообещать одно, а дать другое.
