# Ночной прогон 24.09 — задачи

> Денис спит, работа идёт без вопросов. Не мержить в `main`, прод не трогать.
> Очередь из его слов: бот → веб (далеко позади) → mini-app / Android (только разведка).

## D3 — бот
- [x] Final review fix pass: I3 (лишние чтения при аварии API), I4 (ℹ️ задач дня) — `e52dc18`
- [x] I1 — решение Дениса: паузу красить ⬛, плоские только дни до первого использования; строка в ℹ️
- [x] I2 — `calendar_cell` (цвет / полоска / оба) в ⚙️ → 📌 — `ed6746d`
- [x] push `develop` (`f5362ec`), CI build green, dev-бот на `f5362ec`
- [x] фикс: «только 🟩» + выключенные задачи дня прятали полоску без пути назад (нашёл advisor) — тест + 2 сабботажа красные; строка B3a в чеклисте
- [x] чеклист `docs/_arhiv/proverka-bota-2026-09-24-zadachi-dnya-d3.md` (спека §11 + ширина клетки), открыть в Obsidian

## Веб — задачи дня
- [x] разведка: в вебе задач дня ноль; месячного вида нет (`MonthView` — заглушка) → цвет в шапке недели
- [x] спека `docs/superpowers/specs/2026-09-24-web-day-tasks-design.md` (🟡 без твоего review, вкус — §7)
- [x] план `docs/superpowers/plans/2026-09-24-web-day-tasks.md`
- [x] TDD по плану: 657 → 684 тестов, typecheck/build/lint зелёные, 9 сабботажей красные
  - `00eaaa8` сохранение настроек читает сервер перед записью и идёт по одному (иначе веб откатывал ключи бота)
  - `9160597` страница `/day-tasks`, секция в ⚙️, пункт в навигации (скрыт при выключенных), квадраты в шапке недели
  - e2e `day-tasks.spec.ts`: красный на staging до push, зелёный локально; `settings-race` поправлен под очередь сохранений
- [ ] push, CI (e2e на staging) зелёный
- [x] чеклист прохода веба для Дениса: `docs/_arhiv/proverka-veba-2026-09-24-zadachi-dnya.md`, открыт в Obsidian

## Если веб упёрся
- [x] разведка Telegram mini-app / Android — `docs/superpowers/specs/2026-09-24-mini-app-and-android-razvedka.md`, без кода. Рекомендация: Mini App раньше Android; для Android сначала PWA
- [x] внешние факты (схема initData) → `E:/Projects/100 - Research/prochee/telegram-mini-app-initdata-2026-09-24.md`

## Решения D3, которые я принял за тебя (из ledger'а, полностью)

Продуктовые — на них стоит посмотреть:
- 🟡 `calendar_cell` хранится в `settings.bot.calendar_cell` (`both` / `colour` / `bar`); **число в клетке есть всегда**: без него клетку не выбрать. Если неверно: одна опция добавить.
- 🟡 Выбор клетки стоит на ⚙️ → 📌 (ты выбрал этот вариант), а не в ⚙️ → 🗓, как в спеке §6. Пока задачи дня выключены, ряда нет. Если неверно: переставить один ряд.
- 🟡 Шага «что в клетке» в онбординге нет (спека §6 хотела его с превью): твоё правило «меньше шагов», по умолчанию оба. Если неверно: один экран онбординга.
- 🟡 Фраза в объяснении: «Поменять можно в любой момент» вместо «в ⚙️ Настройках». Тест запрещает направлять человека на кнопку-reply. Если неверно: одно предложение.
- 🟡 Вопрос старым пользователям запоминается в памяти бота. Если ответить на него в вебе, бот увидит ответ только после своего перезапуска.
- 🟡 «Дни до начала» не кэшируются: каждая страница месяца читает настройки один раз (~100 мс), зато выбор всегда свежий.

Технические (стоимость ошибки нулевая, тесты держат):
- 29 мест `HomeInline`, а не 30 · `HomeInline` удалён, `HomeInlineFor` на его месте в help_scan
- fake в `repeat_promise_test` пишет только тела POST · fake `dayAPI` отвечает `[]` на `/api/events`
- `DayTarget` заменён на `DaySettings` + `DayTargetPick` · в `schedule_test` нет `dts_`, и это верно

## Отложенные мелочи из финального ревью D3

✅ Ночью сделаны четыре (тест красный → зелёный, сабботаж красный): **M1** `0197111`, **M7** (кэш 5 мин: веб теперь тоже пишет переключатель), **M4** (легенда месяца), **M2** (экран настроек при сбое чтения говорит об этом, а не ставит галочки по умолчанию). M6 не применим: `before_start` сравнивает даты, без часов и зон. M3 и M5 ждут тебя.

- M1 дата, написанная текстом, проходит мимо проверки «задачи дня выключены»
- M2 экран настроек при сбое чтения показывает значения по умолчанию
- M3 текст знакомства и подписи для старых пользователей можно отполировать
- M4 в легенде месяца нет цветных квадратов
- M5 тест Focus-5 проверяет только вложенный ключ
- M6 нет теста `before_start` с зоной
- M7 кэш `DayTasksOn` не протухает: выключение в вебе бот увидит после перезапуска

## Дальше
- [x] Подзадачи в боте (твой план 23.09, день 2) — `6891e72`, dev-бот, чеклист `docs/_arhiv/proverka-bota-2026-09-24-podzadachi.md`
