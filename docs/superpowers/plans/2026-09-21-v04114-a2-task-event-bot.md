<!-- паспорт: тип=план | статус=действует | строк=1477 | ~токенов=16086 | обновлён=по git -->

# A2 · задача ↔ событие, бот — план реализации

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** в боте задачу можно отправить в календарь полным путём («связать / перенести → серия /
этот раз → недостающее → что чем станет»), событие — превратить в задачу, а связанная задача
остаётся в списке с пометкой «📅 вт 15:00».

**Architecture:** только модуль `bot/`. Два новых файла-сценария (`handlers/toevent.go`,
`handlers/totask.go`) держат выбор пользователя в `FlowData` (flow `to_event` / `to_task`), кнопки
несут короткий код шага, а не данные — длина `callback_data` ≤ 64 байт и синтетический id
`uuid:YYYY-MM-DD` в неё не всегда влезает вместе с шагом. Связь задачи с событием бот узнаёт **без
новой ручки API**: `GET /api/events` уже отдаёт `task_id`, в том числе у вхождений серии (они копия
родителя, `api-go/internal/events/recurrence.go:105`), — одна выборка на окно «сегодня + 60 дней».

**Tech Stack:** Go, `go-telegram-bot-api/v5`, модуль `github.com/zemdenalex/neuroboost-bot`.

**Spec:** `docs/superpowers/specs/2026-09-21-v04114-bot-design.md` §A2, §A3.
**API-контракт:** `docs/superpowers/plans/2026-09-21-v04114-a1-task-event-api.md` Task 2 (convert) и
Task 6 (to-task). Сделано на `develop` 21.09, **не запушено**.

## Global Constraints

- Только `bot/`. Понадобилась правка `api-go` — остановиться и спросить.
- 🔴 Весь видимый текст — через `h.t(chatID, ru, en)` / `i18n.T`: иначе краснеет
  `TestNoUntranslatedUserFacingText`. Ошибки API — через `h.errorText(chatID, err)`, никогда
  `err.Error()` в сообщение (`TestNoHandlerPrintsARawError`).
- 🔴 Каждый новый префикс `callback_data` — в роутере `handler.go` **и** в списке `routed`
  `TestEveryScheduleButtonHasAPrefixTheRouterKnows`; более длинный префикс роутится раньше
  короткого с тем же началом.
- 🔴 Ответ API — конверт `{"data": …}`; `c.do` его не разворачивает, каждый метод клиента объявляет
  поле `Data`. Тест клиента проверяет именно конверт (урок `OccurrenceResult`, 21.09).
- 🔴 Каждый тест обязан мочь упасть: саботаж компилируется, применяется, читается целиком.
  Проверять **ноль `FAIL`**: `go test -count=1 ./... 2>&1 | grep -E '^(--- FAIL|FAIL)'` → пусто.
- Правки — Edit/Write, не массовой заменой (CRLF 21.09 поймал `TestCancelIsHandledOnceAndFirst`).
- `gofmt -l` только своих файлов; `git add` конкретных файлов; без co-author.
- 🔴 Выкат: **сначала** push `develop` (dev API получает A1), **потом** dev-бот на nl-2
  (`/opt/neuroboost-bot`, `neuroboost-dev-bot`). Обратный порядок даст боту 404 на новых ручках.
  Push — только по слову Дениса. Прод не трогать.

## Решения, принятые в плане (сказать Денису в отчёте)

1. 🟡 **«Только этот раз» у события предлагается, только если открыто вхождение** (id вида
   `uuid:дата` — так приходят события из дня календаря и «Сегодня»). Открыта вся серия — кнопки нет,
   строкой сказано почему: неизвестно, какой день имеется в виду.
2. 🟡 **Время в полном пути задача → событие** — те же слоты, что у быстрой кнопки
   (`Сейчас / Через час / Вечером / Завтра`) плюс `✏️ Своё` текстом через существующий разборщик
   (`parse.ParseLine`): «завтра 15:00», «пт 10». Без часа в тексте — переспросить, не угадывать.
   Шаг «день, если у задачи нет срока» спекой предусмотрен отдельно; здесь день входит в слот или
   в текст, отдельного экрана нет.
3. 🟡 **Пометка «📅 …» ищет связанное событие в окне сегодня … +60 дней.** Дальше — пометки нет.
   Не удалось загрузить события — список и карточка показываются **без** пометки (а не ошибкой):
   пометка — украшение, список — главное.
4. «ℹ️ Что это?» в этих экранах — один текст в коде сценария. Реестр объяснений — кусок C, туда
   текст переедет.

## Файлы

| Файл | Что | Задача |
|---|---|---|
| `bot/internal/api/client.go` | `Event.TaskID` | 1 |
| `bot/internal/api/convert.go` | **новый**: `ConvertTask`, `EventToTask` | 1 |
| `bot/internal/api/convert_test.go` | **новый** | 1 |
| `bot/internal/handlers/linked.go` | **новый**: `openTasks`, `linkedEvents`, `whenShort`, `h.linkedFor` | 2 |
| `bot/internal/handlers/linked_test.go` | **новый** | 2, 3 |
| `bot/internal/handlers/tasks.go`, `today.go`, `menu.go`, `calendar.go` | `TODO` → `openTasks`; пометка | 2, 3 |
| `bot/internal/keyboards/keyboards.go` | `TaskActions(lang, id, repeats, linkedEventID)` | 3 |
| `bot/internal/keyboards/convert.go` | **новый**: клавиатуры полного пути | 4, 5 |
| `bot/internal/handlers/toevent.go` | **новый**: задача → событие | 4 |
| `bot/internal/handlers/toevent_test.go` | **новый** | 4 |
| `bot/internal/handlers/totask.go` | **новый**: событие → задача | 5 |
| `bot/internal/handlers/totask_test.go` | **новый** | 5 |
| `bot/internal/keyboards/event.go`, `handlers/eventedit.go` | кнопка «✅ Сделать задачей» | 5 |
| `bot/internal/handlers/handler.go`, `flows.go` | роутинг и текстовый шаг | 4, 5 |
| `bot/internal/handlers/errors.go` | коды `REPEAT_CHOICE_REQUIRED`, `OCCURRENCE_REQUIRED`, `REPEAT_UNSUPPORTED`, `NEEDS_TIME` | 6 |
| `bot/internal/handlers/schedule_test.go` | префиксы и клавиатуры в тripwire | 4, 5 |

---

### Task 1: клиент API — связь в событии и две новые ручки

**Files:**
- Modify: `bot/internal/api/client.go` (`type Event`, ~строка 31)
- Create: `bot/internal/api/convert.go`, `bot/internal/api/convert_test.go`

**Interfaces — Produces:**

```go
// in type Event:
TaskID *string `json:"task_id,omitempty"`

type ConvertReq struct {
	Mode     string `json:"mode"`             // "move" | "link"
	Repeat   string `json:"repeat,omitempty"` // "series" | "once"
	StartsAt string `json:"starts_at"`        // RFC3339
	EndsAt   string `json:"ends_at"`
	AllDay   bool   `json:"all_day,omitempty"`
}
func (c *Client) ConvertTask(token, taskID string, req ConvertReq) (*Event, error)

type ToTaskReq struct {
	Mode       string `json:"mode"`
	Repeat     string `json:"repeat,omitempty"`
	Occurrence string `json:"occurrence,omitempty"`
	DryRun     bool   `json:"dry_run,omitempty"`
}
type PlannedTask struct {
	ID               string   `json:"id,omitempty"`
	Title            string   `json:"title"`
	Description      *string  `json:"description,omitempty"`
	Tags             []string `json:"tags"`
	DueDate          string   `json:"due_date"`
	EstimatedMinutes *int     `json:"estimated_minutes,omitempty"`
	Rrule            *string  `json:"rrule,omitempty"`
}
type ToTaskResult struct {
	Task    PlannedTask `json:"task"`
	Lost    []string    `json:"lost"`
	EventID *string     `json:"event_id"`
	DryRun  bool        `json:"dry_run"`
}
func (c *Client) EventToTask(token, eventID string, req ToTaskReq) (*ToTaskResult, error)
```

- [ ] **Step 1: Падающие тесты** — `bot/internal/api/convert_test.go`:

```go
package api

import (
	"io"
	"net/http"
	"net/http/httptest"
	"strings"
	"testing"
)

// The API wraps every answer in {"data": …} and c.do does not unwrap it. A
// client method that decodes the bare body reads zero values and reports
// success — exactly how OccurrenceResult failed on 21.09.
func TestEventToTaskReadsTheEnvelope(t *testing.T) {
	var path, body string
	srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		path = r.URL.Path
		b, _ := io.ReadAll(r.Body)
		body = string(b)
		w.Header().Set("Content-Type", "application/json")
		_, _ = w.Write([]byte(`{"data":{"task":{"title":"врач","due_date":"2026-10-20",` +
			`"estimated_minutes":70,"tags":[]},"lost":["start_time","color"],"event_id":null,"dry_run":true}}`))
	}))
	defer srv.Close()

	res, err := NewClient(srv.URL).EventToTask("tok", "e1:2026-10-20", ToTaskReq{Mode: "move", Repeat: "once", DryRun: true})
	if err != nil {
		t.Fatalf("err: %v", err)
	}
	if path != "/api/events/e1:2026-10-20/to-task" {
		t.Errorf("path = %q", path)
	}
	if !strings.Contains(body, `"dry_run":true`) || !strings.Contains(body, `"repeat":"once"`) {
		t.Errorf("body = %s", body)
	}
	if res.Task.DueDate != "2026-10-20" || len(res.Lost) != 2 || res.Task.EstimatedMinutes == nil {
		t.Errorf("decoded %+v — the envelope was not read", res)
	}
}

func TestConvertTaskSendsTheChoiceAndReadsTheEvent(t *testing.T) {
	var body string
	srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		b, _ := io.ReadAll(r.Body)
		body = string(b)
		w.Header().Set("Content-Type", "application/json")
		w.WriteHeader(http.StatusCreated)
		_, _ = w.Write([]byte(`{"data":{"id":"ev1","title":"банк","starts_at":"2026-10-20T09:00:00Z",` +
			`"ends_at":"2026-10-20T10:00:00Z","task_id":"t1"}}`))
	}))
	defer srv.Close()

	ev, err := NewClient(srv.URL).ConvertTask("tok", "t1", ConvertReq{
		Mode: "link", Repeat: "series", StartsAt: "2026-10-20T09:00:00Z", EndsAt: "2026-10-20T10:00:00Z",
	})
	if err != nil {
		t.Fatalf("err: %v", err)
	}
	if !strings.Contains(body, `"mode":"link"`) || !strings.Contains(body, `"repeat":"series"`) {
		t.Errorf("body = %s", body)
	}
	if ev.ID != "ev1" || ev.TaskID == nil || *ev.TaskID != "t1" {
		t.Errorf("decoded %+v", ev)
	}
}

// A repeating task sent without a choice must come back as the API's code, so
// the bot can ask — not as a generic failure.
func TestConvertTaskSurfacesRepeatChoiceRequired(t *testing.T) {
	srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		w.Header().Set("Content-Type", "application/json")
		w.WriteHeader(http.StatusBadRequest)
		_, _ = w.Write([]byte(`{"error":{"code":"REPEAT_CHOICE_REQUIRED","message":"x"}}`))
	}))
	defer srv.Close()
	_, err := NewClient(srv.URL).ConvertTask("tok", "t1", ConvertReq{Mode: "link"})
	if CodeOf(err) != "REPEAT_CHOICE_REQUIRED" {
		t.Errorf("code = %q (err %v)", CodeOf(err), err)
	}
}
```

- [ ] **Step 2:** `cd bot && go test -count=1 ./internal/api/` → не компилируется (нет типов) — это «падает».

- [ ] **Step 3: Реализация** — `bot/internal/api/convert.go`:

```go
package api

// Task ↔ event, the two doors api-go opened on 21.09 (plan A1).

// ConvertReq asks POST /api/tasks/{id}/convert to put a task into the calendar.
type ConvertReq struct {
	Mode     string `json:"mode"`             // "move" | "link"
	Repeat   string `json:"repeat,omitempty"` // "series" | "once"; required by the API for a repeating task
	StartsAt string `json:"starts_at"`        // RFC3339
	EndsAt   string `json:"ends_at"`
	AllDay   bool   `json:"all_day,omitempty"`
}

// ConvertTask moves or links a task into the calendar.
func (c *Client) ConvertTask(token, taskID string, req ConvertReq) (*Event, error) {
	var resp struct {
		Data Event `json:"data"`
	}
	if err := c.post("/api/tasks/"+taskID+"/convert", token, req, &resp); err != nil {
		return nil, err
	}
	return &resp.Data, nil
}

// ToTaskReq asks POST /api/events/{id}/to-task.
type ToTaskReq struct {
	Mode       string `json:"mode"`
	Repeat     string `json:"repeat,omitempty"`
	Occurrence string `json:"occurrence,omitempty"`
	DryRun     bool   `json:"dry_run,omitempty"`
}

// PlannedTask is the task an event becomes; ID is empty on a dry run.
type PlannedTask struct {
	ID               string   `json:"id,omitempty"`
	Title            string   `json:"title"`
	Description      *string  `json:"description,omitempty"`
	Tags             []string `json:"tags"`
	DueDate          string   `json:"due_date"`
	EstimatedMinutes *int     `json:"estimated_minutes,omitempty"`
	Rrule            *string  `json:"rrule,omitempty"`
}

// ToTaskResult is the answer; Lost holds api-go's codes (start_time, color,
// location, reminders), which the bot words.
type ToTaskResult struct {
	Task    PlannedTask `json:"task"`
	Lost    []string    `json:"lost"`
	EventID *string     `json:"event_id"`
	DryRun  bool        `json:"dry_run"`
}

// EventToTask turns an event — the whole series, or the occurrence the id
// names — into a task. With DryRun nothing is written: the answer is the card.
//
// The id may be synthetic («uuid:YYYY-MM-DD»); api-go reads the day from it.
func (c *Client) EventToTask(token, eventID string, req ToTaskReq) (*ToTaskResult, error) {
	var resp struct {
		Data ToTaskResult `json:"data"`
	}
	if err := c.post("/api/events/"+eventID+"/to-task", token, req, &resp); err != nil {
		return nil, err
	}
	return &resp.Data, nil
}
```

В `client.go` в `type Event` добавить `TaskID *string \`json:"task_id,omitempty"\``.

- [ ] **Step 4:** `go test -count=1 ./internal/api/` → `ok`.
- [ ] **Step 5: Саботаж:** в `EventToTask` декодировать в `&resp.Data` → `&resp` заменить на прямой `ToTaskResult` без конверта (`var resp ToTaskResult; c.post(..., &resp)`) → `TestEventToTaskReadsTheEnvelope` обязан упасть. Откатить.
- [ ] **Step 6: Commit** — `git add bot/internal/api/client.go bot/internal/api/convert.go bot/internal/api/convert_test.go` · `feat(bot): API client for task↔event — convert and to-task`

---

### Task 2: список показывает `TODO` и `SCHEDULED`

**Files:**
- Create: `bot/internal/handlers/linked.go`, `bot/internal/handlers/linked_test.go`
- Modify: `handlers/tasks.go:19`, `handlers/today.go:33`, `handlers/menu.go:27`, `handlers/calendar.go:217`

**Interfaces — Produces:** `func openTasks(tasks []api.Task) []api.Task`

Почему: быстрый «⏰ Запланировать» ставит `SCHEDULED` (веб на это рассчитывает — решение Дениса
21.09), а все четыре экрана бота просят `GetTasks(token, "TODO")`, — запланированная задача
исчезала из списка. Против решения «связанная задача остаётся, с пометкой».

- [ ] **Step 1: Падающий тест** — `linked_test.go`:

```go
package handlers

import (
	"testing"

	"github.com/zemdenalex/neuroboost-bot/internal/api"
)

// Denis 21.09: a scheduled task stays in the list, marked with its time.
func TestAScheduledTaskIsStillOpen(t *testing.T) {
	got := openTasks([]api.Task{
		{ID: "a", Status: "TODO"}, {ID: "b", Status: "SCHEDULED"},
		{ID: "c", Status: "DONE"}, {ID: "d", Status: "CANCELLED"},
	})
	if len(got) != 2 || got[0].ID != "a" || got[1].ID != "b" {
		t.Errorf("open = %+v, want a and b", got)
	}
}
```

- [ ] **Step 2:** прогнать → не компилируется.
- [ ] **Step 3: Реализация** в `linked.go`:

```go
package handlers

import "github.com/zemdenalex/neuroboost-bot/internal/api"

// openTasks is what the lists show: waiting and scheduled alike.
//
// 🔴 SCHEDULED is set by the quick «⏰ Запланировать» (the web relies on it),
// and every screen here used to ask the API for TODO only — so scheduling a
// task made it vanish from the bot. Denis 21.09: it stays, marked with its time.
func openTasks(tasks []api.Task) []api.Task {
	out := make([]api.Task, 0, len(tasks))
	for _, t := range tasks {
		if t.Status == "TODO" || t.Status == "SCHEDULED" {
			out = append(out, t)
		}
	}
	return out
}
```

В четырёх местах: `h.api.GetTasks(us.AuthToken, "TODO")` → `h.api.GetTasks(us.AuthToken, "")`,
затем `tasks = openTasks(tasks)` сразу после проверки ошибки. Проверить: `grep -rn 'GetTasks(.*"TODO")' bot/internal` → пусто.

- [ ] **Step 4:** `go test -count=1 ./internal/handlers/` → ноль `FAIL`. Существующие тесты экранов
  с фейковым API, отдающим задачи без `status`, могут начать показывать меньше задач — если так,
  фейк дописать `"status":"TODO"`, **а не** ослабить фильтр.
- [ ] **Step 5: Саботаж:** убрать `|| t.Status == "SCHEDULED"` → тест `FAIL`; откатить.
- [ ] **Step 6: Commit** — `fix(bot): a scheduled task stays in the lists`

---

### Task 3: пометка «📅 вт 15:00» в списке и на карточке

**Files:**
- Modify: `bot/internal/handlers/linked.go`, `linked_test.go`, `handlers/tasks.go` (`handleTasks`, `handleTaskAction`)
- Modify: `bot/internal/keyboards/keyboards.go` (`TaskActions`), `handlers/schedule_test.go` (`scheduleKeyboards`)

**Interfaces — Produces:**

```go
// linkedEvents maps a task id to its nearest linked event starting today or later.
func linkedEvents(events []api.Event, from time.Time) map[string]api.Event
// whenShort: «сегодня 15:00» · «завтра 15:00» · «вт 15:00» within 6 days · «22.10 15:00» further.
func whenShort(lang i18n.Lang, t, now time.Time) string
// linkedFor fetches today … +60 days and never fails: an empty map when the API does.
func (h *Handler) linkedFor(chatID int64) map[string]api.Event
// keyboards:
func TaskActions(lang i18n.Lang, taskID string, repeats bool, linkedEventID string) tgbotapi.InlineKeyboardMarkup
```

- [ ] **Step 1: Падающие тесты** — дописать в `linked_test.go`:

```go
func sp(s string) *string { return &s }

func TestTheNearestUpcomingLinkedEventWins(t *testing.T) {
	now := time.Date(2026, 10, 20, 12, 0, 0, 0, time.UTC)
	got := linkedEvents([]api.Event{
		{ID: "old", TaskID: sp("t1"), StartsAt: "2026-10-19T09:00:00Z"}, // yesterday: ignored
		{ID: "far", TaskID: sp("t1"), StartsAt: "2026-10-27T09:00:00Z"},
		{ID: "near", TaskID: sp("t1"), StartsAt: "2026-10-22T15:00:00Z"},
		{ID: "none", StartsAt: "2026-10-21T09:00:00Z"},
	}, now)
	if got["t1"].ID != "near" || len(got) != 1 {
		t.Errorf("linked = %+v, want t1 → near only", got)
	}
}

// Today's event still counts after its hour has passed: «запланирована на
// сегодня 09:00» is true at noon, and dropping it would hide the link.
func TestTodaysLinkedEventCountsAllDay(t *testing.T) {
	now := time.Date(2026, 10, 20, 12, 0, 0, 0, time.UTC)
	got := linkedEvents([]api.Event{{ID: "e", TaskID: sp("t1"), StartsAt: "2026-10-20T09:00:00Z"}}, now)
	if got["t1"].ID != "e" {
		t.Errorf("today's event was dropped: %+v", got)
	}
}

func TestWhenShortNamesTheDayTheWayPeopleDo(t *testing.T) {
	now := time.Date(2026, 10, 20, 12, 0, 0, 0, time.UTC) // Tuesday
	for _, c := range []struct {
		at   time.Time
		want string
	}{
		{time.Date(2026, 10, 20, 15, 0, 0, 0, time.UTC), "сегодня 15:00"},
		{time.Date(2026, 10, 21, 9, 30, 0, 0, time.UTC), "завтра 09:30"},
		{time.Date(2026, 10, 23, 15, 0, 0, 0, time.UTC), "пт 15:00"},
		{time.Date(2026, 11, 2, 15, 0, 0, 0, time.UTC), "02.11 15:00"},
	} {
		if got := whenShort(i18n.RU, c.at, now); got != c.want {
			t.Errorf("%v: %q, want %q", c.at, got, c.want)
		}
	}
}
```

Импорты теста: `time`, `i18n`. ⚠ `weekdayShort(i18n.RU, 4)` должен давать «пт» — сверить таблицу в `stats.go:224`; если там «Пт», поправить ожидание, не таблицу.

- [ ] **Step 2:** прогнать → не компилируется.
- [ ] **Step 3: Реализация** в `linked.go` (импорты `time`, `fmt`, `i18n`):

```go
// linkedEvents finds, for every task, the event its time went into.
//
// The API needs no new door for this: GET /api/events already carries
// task_id, and occurrences of a series are copies of it, so one fetch over a
// window answers every task on the screen.
func linkedEvents(events []api.Event, from time.Time) map[string]api.Event {
	dayStart := time.Date(from.Year(), from.Month(), from.Day(), 0, 0, 0, 0, from.Location())
	out := map[string]api.Event{}
	starts := map[string]time.Time{}
	for _, e := range events {
		if e.TaskID == nil || *e.TaskID == "" {
			continue
		}
		at, err := time.Parse(time.RFC3339, e.StartsAt)
		if err != nil || at.Before(dayStart) {
			continue
		}
		if prev, ok := starts[*e.TaskID]; !ok || at.Before(prev) {
			starts[*e.TaskID] = at
			out[*e.TaskID] = e
		}
	}
	return out
}

func whenShort(lang i18n.Lang, t, now time.Time) string {
	t = t.In(now.Location())
	day := func(x time.Time) time.Time { return time.Date(x.Year(), x.Month(), x.Day(), 0, 0, 0, 0, x.Location()) }
	hhmm := t.Format("15:04")
	switch days := int(day(t).Sub(day(now)).Hours() / 24); {
	case days == 0:
		return i18n.T(lang, "сегодня ", "today ") + hhmm
	case days == 1:
		return i18n.T(lang, "завтра ", "tomorrow ") + hhmm
	case days > 1 && days < 7:
		return weekdayShort(lang, (int(t.Weekday())+6)%7) + " " + hhmm
	default:
		return fmt.Sprintf("%02d.%02d %s", t.Day(), int(t.Month()), hhmm)
	}
}

// linkedWindowDays bounds the look-ahead; a link further out shows no mark.
const linkedWindowDays = 60

func (h *Handler) linkedFor(chatID int64) map[string]api.Event {
	us := h.store.GetOrCreate(chatID)
	now := time.Now().In(h.location(chatID))
	from := time.Date(now.Year(), now.Month(), now.Day(), 0, 0, 0, 0, now.Location())
	events, err := h.api.GetEvents(us.AuthToken,
		from.UTC().Format(time.RFC3339), from.AddDate(0, 0, linkedWindowDays).UTC().Format(time.RFC3339))
	if err != nil {
		// The mark is decoration; the list is the point. No events → no marks,
		// and the list still opens.
		return map[string]api.Event{}
	}
	return linkedEvents(events, now)
}
```

⚠ Сверить `GetEvents(token, start, end string)` — формат дат, который он ждёт (посмотреть, как
его зовёт `calendar.go`), и передавать так же.

`handleTasks`: после сортировки `linked := h.linkedFor(chatID)`, `now := time.Now().In(h.location(chatID))`;
строка текста:

```go
line := fmt.Sprintf("%s %s", format.PriorityEmoji(t.Priority), format.Escape(t.Title))
if ev, ok := linked[t.ID]; ok {
	if at, err := time.Parse(time.RFC3339, ev.StartsAt); err == nil {
		line += "   📅 " + whenShort(h.lang(chatID), at, now)
	}
}
text += line + "\n"
```

`handleTaskAction`: перед отрисовкой `ev, linkedOK := h.linkedFor(chatID)[taskID]`; если есть —
строка `fmt.Sprintf(h.t(chatID, "📅 Запланирована на %s\n", "📅 Scheduled for %s\n"), whenShort(...))`,
и в клавиатуру передать `ev.ID`, иначе `""`.

`keyboards.TaskActions` — новый параметр `linkedEventID string`; первая строка:

```go
first := []tgbotapi.InlineKeyboardButton{
	tgbotapi.NewInlineKeyboardButtonData(i18n.T(lang, "⏰ Запланировать", "⏰ Schedule"), "task_sched_"+taskID),
	tgbotapi.NewInlineKeyboardButtonData(i18n.T(lang, "📅 В календарь", "📅 To calendar"), "t2e_"+taskID),
}
rows := [][]tgbotapi.InlineKeyboardButton{first}
if linkedEventID != "" {
	rows = append(rows, tgbotapi.NewInlineKeyboardRow(
		tgbotapi.NewInlineKeyboardButtonData(i18n.T(lang, "🗓 Открыть событие", "🗓 Open event"), "eve_"+linkedEventID)))
}
// … остальные строки как были
```

⚠ `eve_` — открыть событие (роутится в `eventedit.go:306`); синтетический id + префикс ≤ 64 байт
(4 + 47 = 51). Все вызовы `TaskActions` (`grep -rn "TaskActions(" bot/internal`) дописать четвёртым
аргументом; в `scheduleKeyboards()` — `keyboards.TaskActions(i18n.RU, uuid, true, uuid+":2026-10-22")`
и добавить `"t2e_"`, `"eve_"` в `routed`.

- [ ] **Step 4:** тесты `linked_test.go` + весь пакет → ноль `FAIL`.
- [ ] **Step 5: Саботаж:** в `linkedEvents` убрать `at.Before(dayStart)` → `TestTheNearestUpcomingLinkedEventWins` обязан упасть (выберет «old»). Откатить.
- [ ] **Step 6: Commit** — `feat(bot): a linked task shows when it is scheduled, in the list and on the card`

(Кнопка `t2e_` роутится в Task 4; до него она ведёт в «не понял кнопку». Task 3 и 4 коммитятся
подряд, выкат — только после Task 7.)

---

### Task 4: задача → событие, полный путь

**Files:**
- Create: `bot/internal/keyboards/convert.go`, `bot/internal/handlers/toevent.go`, `bot/internal/handlers/toevent_test.go`
- Modify: `handlers/handler.go` (роутер), `handlers/flows.go` (текстовый шаг), `handlers/schedule_test.go`

**Сценарий** (flow `to_event`, `FlowData`: `taskID`, `mode`, `repeat`, `start` RFC3339, `minutes` int):

| Шаг | Экран | Кнопки (callback) |
|---|---|---|
| 1 | «📅 <название> — как?» + по строке, что будет | `🔗 Связать` `t2m_l` · `➡️ Перенести` `t2m_m` · `ℹ️ Что это?` `t2i` · `❌ Отмена` `t2x` |
| 2 (если задача повторяется) | «Это повторяющаяся задача» | `🔁 Вся серия` `t2r_s` · `1️⃣ Только этот раз` `t2r_o` · `ℹ️` `t2i` · `❌` `t2x` |
| 3 | «Когда?» | `Сейчас` `t2w_now` · `Через час` `t2w_hour` · `Вечером` `t2w_eve` · `Завтра` `t2w_tmr` · `✏️ Своё` `t2c` · `❌` `t2x` |
| 3a (`✏️ Своё`) | «Напиши день и время: «завтра 15:00», «пт 10»» — текст → `parse.ParseLine`; без часа — переспросить | — |
| 4 (если у задачи нет оценки) | «Насколько?» | `15 мин` `t2d_15` · `30 мин` `t2d_30` · `1 ч` `t2d_60` · `2 ч` `t2d_120` · `❌` `t2x` |
| 5 | «Что чем станет» | `✅ Готово` `t2ok` · `ℹ️ Что это?` `t2i` · `❌ Отмена` `t2x` |

Карточка шага 5 (из полей задачи):

```
📅 <b>позвонить в банк</b> → событие
название → название
оценка 1 ч → длительность 1 ч          (или «длительность 30 мин» из шага 4)
срок → день: вт 15:00
описание, теги → как есть
повтор → каждую неделю                 (series) / разово (once) / строки нет
⚠ приоритет → у события нет
🔗 задача останется в списке с пометкой   (link)  /  ➡️ задача исчезнет из списка (move; once — уйдёт один день)
```

**Interfaces — Produces:** `const toEventFlow = "to_event"`;
`func (h *Handler) handleToEventStart(chatID int64, messageID int, taskID string)`;
`func (h *Handler) handleToEventStep(chatID int64, messageID int, data string)` — один вход для
всех `t2*` кроме старта; `func (h *Handler) handleToEventText(chatID int64, text string)`;
`func toEventCard(lang i18n.Lang, t api.Task, mode, repeat string, start time.Time, minutes int, now time.Time) string`.
Клавиатуры в `keyboards/convert.go`: `ConvertHow(lang, prefix)`, `ConvertRepeat(lang, prefix)`,
`ToEventWhen(lang)`, `ToEventDuration(lang)`, `ConvertConfirm(lang, prefix)` — `prefix` = `"t2"` или `"e2"`,
чтобы Task 5 использовал те же кнопки.

- [ ] **Step 1: Падающие тесты** — `toevent_test.go`. Фейковый API: мультиплексор по методу+пути,
  запоминает тело `POST …/convert`.

```go
package handlers

import (
	"io"
	"net/http"
	"net/http/httptest"
	"strings"
	"testing"
	"time"

	"github.com/zemdenalex/neuroboost-bot/internal/api"
	"github.com/zemdenalex/neuroboost-bot/internal/config"
	"github.com/zemdenalex/neuroboost-bot/internal/i18n"
	"github.com/zemdenalex/neuroboost-bot/internal/state"
)

type convertFake struct {
	convertBody string
	toTaskBody  string
}

func convertAPI(t *testing.T, tasksJSON, eventJSON string) (*Handler, *fakeTelegram, *convertFake) {
	t.Helper()
	cf := &convertFake{}
	srv := httptest.NewServer(http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		w.Header().Set("Content-Type", "application/json")
		b, _ := io.ReadAll(r.Body)
		switch {
		case r.Method == http.MethodPost && strings.HasSuffix(r.URL.Path, "/convert"):
			cf.convertBody = string(b)
			w.WriteHeader(http.StatusCreated)
			_, _ = w.Write([]byte(`{"data":{"id":"ev1","title":"банк","starts_at":"2026-10-20T09:00:00Z","ends_at":"2026-10-20T10:00:00Z"}}`))
		case r.Method == http.MethodPost && strings.HasSuffix(r.URL.Path, "/to-task"):
			cf.toTaskBody = string(b)
			dry := strings.Contains(string(b), `"dry_run":true`)
			id := `"id":"t9",`
			if dry {
				id = ""
			}
			_, _ = w.Write([]byte(`{"data":{"task":{` + id + `"title":"врач","due_date":"2026-10-20","estimated_minutes":70,"tags":[]},` +
				`"lost":["start_time","color"],"event_id":null,"dry_run":` + map[bool]string{true: "true", false: "false"}[dry] + `}}`))
		case strings.HasPrefix(r.URL.Path, "/api/events/"):
			_, _ = w.Write([]byte(`{"data":` + eventJSON + `}`))
		case r.URL.Path == "/api/events":
			_, _ = w.Write([]byte(`{"data":[]}`))
		default: // /api/tasks
			_, _ = w.Write([]byte(`{"data":[` + tasksJSON + `]}`))
		}
	}))
	t.Cleanup(srv.Close)
	bot, fake := newFakeTelegram(t)
	return New(bot, api.NewClient(srv.URL), state.NewStore(), config.Config{}), fake, cf
}

const plainTask = `{"id":"t1","title":"банк","status":"TODO","priority":2,"estimated_minutes":60}`
const seriesTaskJSON = `{"id":"t1","title":"таблетки","status":"TODO","priority":3,"rrule":"FREQ=DAILY"}`

// The whole path, pressed as a user would: link → tomorrow → done.
func TestToEventLinkSendsWhatWasChosen(t *testing.T) {
	h, fake, cf := convertAPI(t, plainTask, `{}`)
	const chat = 900
	h.handleToEventStart(chat, 0, "t1")
	h.handleToEventStep(chat, 0, "t2m_l")
	h.handleToEventStep(chat, 0, "t2w_tmr")
	// estimate is 60 → no duration step; the card is next
	card := fake.last(t)
	if !strings.Contains(card.Text, "приоритет") || !strings.Contains(card.Markup, "t2ok") {
		t.Fatalf("expected the mapping card with a priority warning, got %q / %s", card.Text, card.Markup)
	}
	h.handleToEventStep(chat, 0, "t2ok")

	if !strings.Contains(cf.convertBody, `"mode":"link"`) {
		t.Errorf("convert body = %s", cf.convertBody)
	}
	if strings.Contains(cf.convertBody, `"repeat"`) {
		t.Errorf("a one-off task sent a repeat choice: %s", cf.convertBody)
	}
	var body struct{ StartsAt, EndsAt string }
	_ = jsonUnmarshal(cf.convertBody, &body)
	s, _ := time.Parse(time.RFC3339, body.StartsAt)
	e, _ := time.Parse(time.RFC3339, body.EndsAt)
	if e.Sub(s) != time.Hour {
		t.Errorf("length = %v, want the task's 60-minute estimate", e.Sub(s))
	}
}

// 🔴 A repeating task is ASKED «вся серия или этот раз» — Denis 21.09, «уточнять».
func TestToEventAsksSeriesOrOnceForARepeatingTask(t *testing.T) {
	h, fake, cf := convertAPI(t, seriesTaskJSON, `{}`)
	const chat = 901
	h.handleToEventStart(chat, 0, "t1")
	h.handleToEventStep(chat, 0, "t2m_m")
	if m := fake.last(t).Markup; !strings.Contains(m, "t2r_s") || !strings.Contains(m, "t2r_o") {
		t.Fatalf("no series/once question after the mode: %s", m)
	}
	h.handleToEventStep(chat, 0, "t2r_o")
	h.handleToEventStep(chat, 0, "t2w_tmr")
	h.handleToEventStep(chat, 0, "t2d_30") // no estimate → duration asked
	h.handleToEventStep(chat, 0, "t2ok")
	if !strings.Contains(cf.convertBody, `"repeat":"once"`) || !strings.Contains(cf.convertBody, `"mode":"move"`) {
		t.Errorf("convert body = %s", cf.convertBody)
	}
}

// A time typed without an hour is asked again, never guessed — a task has a
// due DAY, and inventing 09:00 puts it in the calendar at an hour nobody chose.
func TestToEventCustomTimeWithoutAnHourIsAskedAgain(t *testing.T) {
	h, fake, cf := convertAPI(t, plainTask, `{}`)
	const chat = 902
	h.handleToEventStart(chat, 0, "t1")
	h.handleToEventStep(chat, 0, "t2m_l")
	h.handleToEventStep(chat, 0, "t2c")
	h.handleToEventText(chat, "завтра")
	if cf.convertBody != "" {
		t.Fatalf("converted with no hour: %s", cf.convertBody)
	}
	if !strings.Contains(fake.last(t).Text, "время") {
		t.Errorf("did not ask for the hour again: %q", fake.last(t).Text)
	}
	h.handleToEventText(chat, "завтра 15:00")
	if !strings.Contains(fake.last(t).Markup, "t2ok") {
		t.Errorf("a full time did not reach the card: %s", fake.last(t).Markup)
	}
}

// A button from an abandoned flow does nothing to the calendar.
func TestAStaleToEventButtonWritesNothing(t *testing.T) {
	h, _, cf := convertAPI(t, plainTask, `{}`)
	h.handleToEventStep(903, 0, "t2ok")
	if cf.convertBody != "" {
		t.Errorf("a stale ✅ converted: %s", cf.convertBody)
	}
}
```

`jsonUnmarshal` — маленький помощник в том же файле: `func jsonUnmarshal(s string, v any) error { return json.Unmarshal([]byte(s), v) }` (импорт `encoding/json`).
⚠ Ассерты текста «приоритет» / «время» — русские подстроки из `h.t`; язык чата по умолчанию —
проверить `h.lang` на новом чате (если EN — выставить RU через store, как делают соседние тесты).

- [ ] **Step 2:** прогнать → не компилируется.

- [ ] **Step 3: Клавиатуры** — `keyboards/convert.go`:

```go
package keyboards

import (
	tgbotapi "github.com/go-telegram-bot-api/telegram-bot-api/v5"

	"github.com/zemdenalex/neuroboost-bot/internal/i18n"
)

// The full «в календарь / сделать задачей» path — one set of buttons for both
// directions, told apart by prefix: "t2" (task → event) or "e2" (event → task).
// The choice lives in the flow, not in the button: a synthetic event id plus a
// step code does not always fit Telegram's 64 bytes.

func cancelInfoRow(lang i18n.Lang, prefix string) []tgbotapi.InlineKeyboardButton {
	return tgbotapi.NewInlineKeyboardRow(
		tgbotapi.NewInlineKeyboardButtonData(i18n.T(lang, "ℹ️ Что это?", "ℹ️ What is this?"), prefix+"i"),
		tgbotapi.NewInlineKeyboardButtonData(i18n.T(lang, "❌ Отмена", "❌ Cancel"), prefix+"x"),
	)
}

func ConvertHow(lang i18n.Lang, prefix string) tgbotapi.InlineKeyboardMarkup {
	return tgbotapi.NewInlineKeyboardMarkup(
		tgbotapi.NewInlineKeyboardRow(
			tgbotapi.NewInlineKeyboardButtonData(i18n.T(lang, "🔗 Связать", "🔗 Link"), prefix+"m_l"),
			tgbotapi.NewInlineKeyboardButtonData(i18n.T(lang, "➡️ Перенести", "➡️ Move"), prefix+"m_m"),
		),
		cancelInfoRow(lang, prefix),
	)
}

// ConvertRepeat asks which part of a series. withOnce is false when nobody
// knows which day «this once» would be (event → task opened on the series).
func ConvertRepeat(lang i18n.Lang, prefix string, withOnce bool) tgbotapi.InlineKeyboardMarkup {
	row := tgbotapi.NewInlineKeyboardRow(
		tgbotapi.NewInlineKeyboardButtonData(i18n.T(lang, "🔁 Вся серия", "🔁 Whole series"), prefix+"r_s"))
	if withOnce {
		row = append(row, tgbotapi.NewInlineKeyboardButtonData(i18n.T(lang, "1️⃣ Только этот раз", "1️⃣ Just this once"), prefix+"r_o"))
	}
	return tgbotapi.NewInlineKeyboardMarkup(row, cancelInfoRow(lang, prefix))
}

func ToEventWhen(lang i18n.Lang) tgbotapi.InlineKeyboardMarkup {
	return tgbotapi.NewInlineKeyboardMarkup(
		tgbotapi.NewInlineKeyboardRow(
			tgbotapi.NewInlineKeyboardButtonData(i18n.T(lang, "Сейчас", "Now"), "t2w_now"),
			tgbotapi.NewInlineKeyboardButtonData(i18n.T(lang, "Через час", "In an hour"), "t2w_hour"),
		),
		tgbotapi.NewInlineKeyboardRow(
			tgbotapi.NewInlineKeyboardButtonData(i18n.T(lang, "Вечером", "Tonight"), "t2w_eve"),
			tgbotapi.NewInlineKeyboardButtonData(i18n.T(lang, "Завтра", "Tomorrow"), "t2w_tmr"),
			tgbotapi.NewInlineKeyboardButtonData(i18n.T(lang, "✏️ Своё", "✏️ Custom"), "t2c"),
		),
		cancelInfoRow(lang, "t2"),
	)
}

func ToEventDuration(lang i18n.Lang) tgbotapi.InlineKeyboardMarkup {
	return tgbotapi.NewInlineKeyboardMarkup(
		tgbotapi.NewInlineKeyboardRow(
			tgbotapi.NewInlineKeyboardButtonData(i18n.T(lang, "15 мин", "15 min"), "t2d_15"),
			tgbotapi.NewInlineKeyboardButtonData(i18n.T(lang, "30 мин", "30 min"), "t2d_30"),
			tgbotapi.NewInlineKeyboardButtonData(i18n.T(lang, "1 ч", "1 h"), "t2d_60"),
			tgbotapi.NewInlineKeyboardButtonData(i18n.T(lang, "2 ч", "2 h"), "t2d_120"),
		),
		cancelInfoRow(lang, "t2"),
	)
}

func ConvertConfirm(lang i18n.Lang, prefix string) tgbotapi.InlineKeyboardMarkup {
	return tgbotapi.NewInlineKeyboardMarkup(
		tgbotapi.NewInlineKeyboardRow(
			tgbotapi.NewInlineKeyboardButtonData(i18n.T(lang, "✅ Готово", "✅ Done"), prefix+"ok")),
		cancelInfoRow(lang, prefix),
	)
}
```

⚠ Сверить с `TaskScheduleWhen` / `TaskScheduleDuration` — если в быстром пути подписи слотов
другие, взять **те же слова**: одна и та же кнопка не должна называться по-разному.

- [ ] **Step 4: Сценарий** — `handlers/toevent.go`:

```go
package handlers

import (
	"fmt"
	"strconv"
	"strings"
	"time"

	"github.com/zemdenalex/neuroboost-bot/internal/api"
	"github.com/zemdenalex/neuroboost-bot/internal/format"
	"github.com/zemdenalex/neuroboost-bot/internal/i18n"
	"github.com/zemdenalex/neuroboost-bot/internal/keyboards"
	"github.com/zemdenalex/neuroboost-bot/internal/parse"
)

// Task → event, the full path (spec 21.09 §A2). Denis's shape «1 + 3»: ask
// move or link · show what becomes what · ask only what is missing. The quick
// «⏰ Запланировать» stays two taps; this is the path that explains itself.

const toEventFlow = "to_event"

// toEventDurations is the closed set t2d_ accepts; callback data is user input.
var toEventDurations = map[int]bool{15: true, 30: true, 60: true, 120: true}

func (h *Handler) handleToEventStart(chatID int64, messageID int, taskID string) {
	task, ok := h.taskByID(chatID, taskID)
	if !ok {
		return
	}
	us := h.store.GetOrCreate(chatID)
	us.CurrentFlow = toEventFlow
	us.FlowStep = "how"
	us.FlowData = map[string]any{"taskID": taskID}
	h.editOrSend(chatID, messageID, fmt.Sprintf(h.t(chatID,
		"📅 <b>%s</b> — в календарь\n\n🔗 <b>Связать</b> — задача останется в списке, у события будет её время и напоминания.\n➡️ <b>Перенести</b> — останется только событие.",
		"📅 <b>%s</b> — to the calendar\n\n🔗 <b>Link</b> — the task stays in the list; the event carries its time and reminders.\n➡️ <b>Move</b> — only the event remains."),
		format.Escape(task.Title)), keyboards.ConvertHow(h.lang(chatID), "t2"))
}

// handleToEventStep is every t2* button after the first.
func (h *Handler) handleToEventStep(chatID int64, messageID int, data string) {
	us := h.store.GetOrCreate(chatID)
	if data == "t2x" {
		h.store.ClearFlow(chatID)
		h.editOrSend(chatID, messageID, h.t(chatID, "Отменено.", "Cancelled."), keyboards.BackToTasks(h.lang(chatID)))
		return
	}
	if data == "t2i" {
		h.sendText(chatID, convertExplanation(h.lang(chatID)))
		return
	}
	if us.CurrentFlow != toEventFlow {
		// A button from a flow that is gone: say so, write nothing.
		h.editOrSend(chatID, messageID, h.t(chatID,
			"Это меню устарело — открой задачу заново.", "This menu is out of date — open the task again."),
			keyboards.BackToTasks(h.lang(chatID)))
		return
	}
	taskID, _ := us.FlowData["taskID"].(string)
	task, ok := h.taskByID(chatID, taskID)
	if !ok {
		h.store.ClearFlow(chatID)
		return
	}

	switch {
	case data == "t2m_l" || data == "t2m_m":
		us.FlowData["mode"] = map[string]string{"t2m_l": "link", "t2m_m": "move"}[data]
		if task.Repeats() {
			h.editOrSend(chatID, messageID, h.t(chatID,
				"🔁 Это повторяющаяся задача.\n\n<b>Вся серия</b> — событие тоже будет повторяться.\n<b>Только этот раз</b> — в календарь уйдёт один день, серия задачи останется.",
				"🔁 This task repeats.\n\n<b>Whole series</b> — the event repeats too.\n<b>Just this once</b> — one day goes to the calendar; the series stays a task."),
				keyboards.ConvertRepeat(h.lang(chatID), "t2", true))
			return
		}
		h.askToEventWhen(chatID, messageID)
	case data == "t2r_s" || data == "t2r_o":
		us.FlowData["repeat"] = map[string]string{"t2r_s": "series", "t2r_o": "once"}[data]
		h.askToEventWhen(chatID, messageID)
	case strings.HasPrefix(data, "t2w_"):
		slot := strings.TrimPrefix(data, "t2w_")
		if !scheduleSlots[slot] {
			return
		}
		start, _ := scheduleStart(slot, time.Now(), h.location(chatID))
		h.toEventHaveStart(chatID, messageID, task, start)
	case data == "t2c":
		us.FlowStep = "time_text"
		h.editOrSend(chatID, messageID, h.t(chatID,
			"✏️ Напиши день и время: «завтра 15:00», «пт 10:30». Или «cancel».",
			"✏️ Type a day and time: «tomorrow 15:00», «fri 10:30». Or «cancel»."), nil)
	case strings.HasPrefix(data, "t2d_"):
		minutes, err := strconv.Atoi(strings.TrimPrefix(data, "t2d_"))
		if err != nil || !toEventDurations[minutes] {
			return
		}
		us.FlowData["minutes"] = minutes
		h.showToEventCard(chatID, messageID, task)
	case data == "t2ok":
		h.finishToEvent(chatID, messageID, task)
	}
}

func (h *Handler) askToEventWhen(chatID int64, messageID int) {
	h.editOrSend(chatID, messageID, h.t(chatID, "🕐 Когда?", "🕐 When?"), keyboards.ToEventWhen(h.lang(chatID)))
}

// handleToEventText is the «✏️ Своё» answer.
func (h *Handler) handleToEventText(chatID int64, text string) {
	us := h.store.GetOrCreate(chatID)
	taskID, _ := us.FlowData["taskID"].(string)
	task, ok := h.taskByID(chatID, taskID)
	if !ok {
		h.store.ClearFlow(chatID)
		return
	}
	loc := h.location(chatID)
	now := time.Now().In(loc)
	p := parse.ParseLine(text, now)
	if !p.Draft.HasTime {
		// 🔴 A day without an hour is asked again, never completed with a
		// guess — the flow stays open, the same rule the wizard follows.
		h.sendText(chatID, h.t(chatID,
			"Нужно и время, например «завтра 15:00». Или «cancel».",
			"I need a time too, e.g. «tomorrow 15:00». Or «cancel»."))
		return
	}
	day := p.Draft.Day
	if !p.Draft.HasDay {
		day = time.Date(now.Year(), now.Month(), now.Day(), 0, 0, 0, 0, loc)
	}
	us.FlowStep = "card"
	h.toEventHaveStart(chatID, 0, task, day.Add(p.Draft.Start))
}

func (h *Handler) toEventHaveStart(chatID int64, messageID int, task api.Task, start time.Time) {
	us := h.store.GetOrCreate(chatID)
	us.FlowData["start"] = start.Format(time.RFC3339)
	if task.EstimatedMinutes > 0 {
		us.FlowData["minutes"] = task.EstimatedMinutes
		h.showToEventCard(chatID, messageID, task)
		return
	}
	h.editOrSend(chatID, messageID, h.t(chatID,
		"⏱ Насколько? У задачи нет оценки.", "⏱ How long? The task has no estimate."),
		keyboards.ToEventDuration(h.lang(chatID)))
}

func (h *Handler) showToEventCard(chatID int64, messageID int, task api.Task) {
	us := h.store.GetOrCreate(chatID)
	start, _ := time.Parse(time.RFC3339, fmt.Sprint(us.FlowData["start"]))
	minutes, _ := us.FlowData["minutes"].(int)
	mode, _ := us.FlowData["mode"].(string)
	repeat, _ := us.FlowData["repeat"].(string)
	now := time.Now().In(h.location(chatID))
	h.editOrSend(chatID, messageID,
		toEventCard(h.lang(chatID), task, mode, repeat, start.In(now.Location()), minutes, now),
		keyboards.ConvertConfirm(h.lang(chatID), "t2"))
}

// toEventCard is the «что чем станет» screen. Pure, so its lines are tested.
func toEventCard(lang i18n.Lang, t api.Task, mode, repeat string, start time.Time, minutes int, now time.Time) string {
	var b strings.Builder
	fmt.Fprintf(&b, i18n.T(lang, "📅 <b>%s</b> → событие\n\n", "📅 <b>%s</b> → event\n\n"), format.Escape(t.Title))
	b.WriteString(i18n.T(lang, "название → название\n", "title → title\n"))
	fmt.Fprintf(&b, i18n.T(lang, "длительность: %s\n", "length: %s\n"), format.Duration(minutes))
	fmt.Fprintf(&b, i18n.T(lang, "когда: %s\n", "when: %s\n"), whenShort(lang, start, now))
	b.WriteString(i18n.T(lang, "описание, теги → как есть\n", "description, tags → as they are\n"))
	switch {
	case t.Repeats() && repeat == "series":
		b.WriteString(i18n.T(lang, "🔁 повтор → событие повторяется\n", "🔁 repeat → the event repeats\n"))
	case t.Repeats():
		b.WriteString(i18n.T(lang, "1️⃣ в календарь уходит один день\n", "1️⃣ one day goes to the calendar\n"))
	}
	b.WriteString(i18n.T(lang, "⚠ приоритет → у события нет\n", "⚠ priority → events have none\n"))
	switch {
	case mode == "link":
		b.WriteString(i18n.T(lang, "\n🔗 Задача останется в списке с пометкой времени.", "\n🔗 The task stays in the list, marked with the time."))
	case t.Repeats() && repeat == "once":
		b.WriteString(i18n.T(lang, "\n➡️ Этот день уйдёт из серии задачи, серия останется.", "\n➡️ This day leaves the task's series; the series stays."))
	default:
		b.WriteString(i18n.T(lang, "\n➡️ Задача исчезнет из списка, останется событие.", "\n➡️ The task leaves the list; the event remains."))
	}
	return b.String()
}

func (h *Handler) finishToEvent(chatID int64, messageID int, task api.Task) {
	us := h.store.GetOrCreate(chatID)
	start, err := time.Parse(time.RFC3339, fmt.Sprint(us.FlowData["start"]))
	minutes, _ := us.FlowData["minutes"].(int)
	mode, _ := us.FlowData["mode"].(string)
	repeat, _ := us.FlowData["repeat"].(string)
	if err != nil || minutes <= 0 || mode == "" {
		h.store.ClearFlow(chatID)
		h.editOrSend(chatID, messageID, h.t(chatID,
			"Это меню устарело — открой задачу заново.", "This menu is out of date — open the task again."),
			keyboards.BackToTasks(h.lang(chatID)))
		return
	}
	end := start.Add(time.Duration(minutes) * time.Minute)
	ev, err := h.api.ConvertTask(us.AuthToken, task.ID, api.ConvertReq{
		Mode: mode, Repeat: repeat,
		StartsAt: start.UTC().Format(time.RFC3339), EndsAt: end.UTC().Format(time.RFC3339),
	})
	h.store.ClearFlow(chatID)
	if err != nil {
		h.editOrSend(chatID, messageID,
			h.t(chatID, "❌ Не получилось: ", "❌ Did not work: ")+format.Escape(h.errorText(chatID, err)),
			keyboards.BackToTasks(h.lang(chatID)))
		return
	}
	now := time.Now().In(h.location(chatID))
	h.editOrSend(chatID, messageID,
		fmt.Sprintf(h.t(chatID, "✅ <b>В календаре</b>\n🕐 %s", "✅ <b>On the calendar</b>\n🕐 %s"),
			humanRange(h.lang(chatID), start.In(now.Location()), end.In(now.Location()), now)),
		keyboards.EventCard(h.lang(chatID), ev.ID))
}

// convertExplanation is the «ℹ️ Что это?» of both directions. Moves to the
// screen-explanation registry with piece C.
func convertExplanation(lang i18n.Lang) string {
	return i18n.T(lang,
		"ℹ️ <b>Задача и событие</b>\n\nЗадача — что сделать. Событие — когда.\n\n🔗 <b>Связать</b> — оба живут: событие даёт время и напоминания, задача закрывается только своим «Готово».\n➡️ <b>Перенести</b> — остаётся одно из двух.\n\nПотери называются до подтверждения. ❌ Отмена ничего не меняет.",
		"ℹ️ <b>Tasks and events</b>\n\nA task is what to do. An event is when.\n\n🔗 <b>Link</b> — both live: the event carries the time and reminders; the task closes only with its own «Done».\n➡️ <b>Move</b> — one of the two remains.\n\nAnything lost is named before you confirm. ❌ Cancel changes nothing.")
}
```

⚠ `h.taskByID(chatID, id) (api.Task, bool)` уже есть (`task_repeat_edit.go:31`) — сверить
сигнатуру; если возвращает иначе — подогнать вызовы, не дублировать функцию.
⚠ `editOrSend(…, nil)` — проверить, что `nil`-клавиатура допустима (фейк Telegram отклоняет
`"inline_keyboard":null`). Если нет — на шаге `t2c` передать `keyboards.ConvertConfirm`-подобную
клавиатуру с одной `❌ Отмена` (`cancelInfoRow` экспортировать как `ConvertCancel(lang, prefix)`).
⚠ `format.Duration`, `humanRange`, `keyboards.EventCard` — существующие; сверить имена.

- [ ] **Step 5: Роутинг и текст.** `handler.go`, рядом с `task_sched_`:

```go
case strings.HasPrefix(data, "t2e_"):
	h.handleToEventStart(chatID, cb.Message.MessageID, strings.TrimPrefix(data, "t2e_"))
case strings.HasPrefix(data, "t2"):
	h.handleToEventStep(chatID, cb.Message.MessageID, data)
```

⚠ `t2e_` **раньше** `t2`. Проверить, что ни один существующий префикс не начинается с `t2`
(`grep -rn '"t2' bot/internal/keyboards`) — иначе общий `t2` его проглотит.

`flows.go`, в `switch us.CurrentFlow`: `case toEventFlow: h.handleToEventText(chatID, text)`.

`schedule_test.go`: в `routed` добавить `"t2e_", "t2"`; в `scheduleKeyboards()` —
`"ConvertHow": keyboards.ConvertHow(i18n.RU, "t2")`, `"ConvertRepeat": keyboards.ConvertRepeat(i18n.RU, "t2", true)`,
`"ToEventWhen": keyboards.ToEventWhen(i18n.RU)`, `"ToEventDuration": keyboards.ToEventDuration(i18n.RU)`,
`"ConvertConfirm": keyboards.ConvertConfirm(i18n.RU, "t2")`.

- [ ] **Step 6:** `cd bot && go build ./... && go vet ./... && go test -count=1 ./... 2>&1 | grep -E '^(--- FAIL|FAIL)'` → пусто (включая сканы i18n, raw error, router).
- [ ] **Step 7: Саботаж — по одному:**
  1. в `t2m_l` / `t2m_m` убрать ветку `task.Repeats()` → `TestToEventAsksSeriesOrOnceForARepeatingTask` `FAIL`;
  2. в `handleToEventText` убрать проверку `HasTime` → `TestToEventCustomTimeWithoutAnHourIsAskedAgain` `FAIL`;
  3. убрать проверку `us.CurrentFlow != toEventFlow` → `TestAStaleToEventButtonWritesNothing` `FAIL`.
- [ ] **Step 8: Commit** — `git add` файлы задачи · `feat(bot): task → event, the full path — link or move, series or once, what becomes what`

---

### Task 5: событие → задача

**Files:**
- Create: `bot/internal/handlers/totask.go`, `bot/internal/handlers/totask_test.go`
- Modify: `keyboards/event.go` (`EventEditor`, `EventCard`), `handlers/eventedit.go` (`handleEventCard` передаёт сырой id), `handler.go`, `schedule_test.go`

**Сценарий** (flow `to_task`, `FlowData`: `eventID` — как пришёл, может быть `uuid:дата`; `mode`; `repeat`):

| Шаг | Экран | Кнопки |
|---|---|---|
| 1 | «✅ <название> — сделать задачей» + что будет | `e2m_l` · `e2m_m` · `e2i` · `e2x` |
| 2 (если событие повторяется) | «Это повторяющееся событие» (+ «Открыта вся серия — один день выбрать нельзя; открой событие из дня календаря», если id не вхождение) | `e2r_s` · `e2r_o` (только для вхождения) · `e2i` · `e2x` |
| 3 | карточка из `dry_run` | `e2ok` · `e2i` · `e2x` |

Карточка (из `ToTaskResult`):

```
✅ <b>врач</b> → задача
срок: вт, 20 октября
оценка: 1 ч 10 мин
описание, теги → как есть
🔁 повтор → задача повторяется          (series)
⚠ час начала → у задачи нет
⚠ цвет → у задачи нет
⚠ место → у задачи нет
⚠ напоминания → у задачи будут свои, по умолчанию
🔗 событие останется и будет указывать на задачу   (link)  /  ➡️ событие удалится (move; once — один день)
```

**Interfaces — Produces:** `const toTaskFlow = "to_task"`;
`func (h *Handler) handleToTaskStart(chatID int64, messageID int, rawEventID string)`;
`func (h *Handler) handleToTaskStep(chatID int64, messageID int, data string)`;
`func toTaskCard(lang i18n.Lang, title string, r api.ToTaskResult, mode, repeat string) string`;
`func lostLine(lang i18n.Lang, code string) string`.
`keyboards.EventEditor(lang i18n.Lang, id, rawID string)` и `keyboards.EventCard(lang i18n.Lang, id string)`
получают строку `✅ Сделать задачей` → `e2t_`+rawID / `e2t_`+id.

- [ ] **Step 1: Падающие тесты** — `totask_test.go` (использует `convertAPI` из Task 4):

```go
package handlers

import (
	"strings"
	"testing"

	"github.com/zemdenalex/neuroboost-bot/internal/api"
	"github.com/zemdenalex/neuroboost-bot/internal/i18n"
)

const seriesEvent = `{"id":"e1","title":"планёрка","starts_at":"2026-10-20T09:00:00Z","ends_at":"2026-10-20T10:00:00Z","rrule":"FREQ=DAILY","tags":[]}`
const plainEvent = `{"id":"e1","title":"врач","starts_at":"2026-10-20T11:50:00Z","ends_at":"2026-10-20T13:00:00Z","color":"#f00","tags":[]}`

// The card is the API's dry run, not the bot's guess: the mapping lives in
// one place (spec §A1), and the losses it names are the ones shown.
func TestToTaskShowsTheDryRunBeforeWriting(t *testing.T) {
	h, fake, cf := convertAPI(t, "", plainEvent)
	const chat = 910
	h.handleToTaskStart(chat, 0, "e1")
	h.handleToTaskStep(chat, 0, "e2m_m")
	card := fake.last(t)
	if !strings.Contains(cf.toTaskBody, `"dry_run":true`) {
		t.Fatalf("the card was not built from a dry run: %s", cf.toTaskBody)
	}
	if !strings.Contains(card.Text, "час начала") || !strings.Contains(card.Text, "цвет") {
		t.Errorf("losses not named on the card: %q", card.Text)
	}
	h.handleToTaskStep(chat, 0, "e2ok")
	if strings.Contains(cf.toTaskBody, `"dry_run":true`) {
		t.Errorf("✅ sent another dry run: %s", cf.toTaskBody)
	}
	if !strings.Contains(cf.toTaskBody, `"mode":"move"`) {
		t.Errorf("real call body = %s", cf.toTaskBody)
	}
}

// «Только этот раз» exists only when a DAY is known — an occurrence id.
func TestOnceIsOfferedOnlyForAnOccurrence(t *testing.T) {
	h, fake, _ := convertAPI(t, "", seriesEvent)
	h.handleToTaskStart(911, 0, "e1")
	h.handleToTaskStep(911, 0, "e2m_l")
	if strings.Contains(fake.last(t).Markup, "e2r_o") {
		t.Errorf("«this once» offered on the whole series: %s", fake.last(t).Markup)
	}

	h2, fake2, cf2 := convertAPI(t, "", seriesEvent)
	h2.handleToTaskStart(912, 0, "e1:2026-10-22")
	h2.handleToTaskStep(912, 0, "e2m_m")
	if !strings.Contains(fake2.last(t).Markup, "e2r_o") {
		t.Fatalf("no «this once» on an occurrence: %s", fake2.last(t).Markup)
	}
	h2.handleToTaskStep(912, 0, "e2r_o")
	if !strings.Contains(cf2.toTaskBody, `"repeat":"once"`) {
		t.Errorf("dry run body = %s", cf2.toTaskBody)
	}
}

func TestEveryLostCodeHasWords(t *testing.T) {
	for _, code := range []string{"start_time", "color", "location", "reminders"} {
		if l := lostLine(i18n.RU, code); l == "" || strings.Contains(l, code) {
			t.Errorf("%s → %q: a code the user would read raw", code, l)
		}
	}
	_ = api.ToTaskResult{}
}
```

⚠ Путь фейка: `EventToTask` шлёт на `/api/events/e1:2026-10-22/to-task`, и `convertAPI` ловит его
по суффиксу `/to-task` **раньше** ветки `/api/events/` — порядок `case` в фейке это уже учитывает.

- [ ] **Step 2:** прогнать → не компилируется.

- [ ] **Step 3: Реализация** — `handlers/totask.go`:

```go
package handlers

import (
	"fmt"
	"strings"
	"time"

	"github.com/zemdenalex/neuroboost-bot/internal/api"
	"github.com/zemdenalex/neuroboost-bot/internal/format"
	"github.com/zemdenalex/neuroboost-bot/internal/i18n"
	"github.com/zemdenalex/neuroboost-bot/internal/keyboards"
)

// Event → task (spec 21.09 §A2). The card is the API's dry run: the mapping —
// which day, how long, what is lost — is computed once, in api-go, in the
// user's zone, and the bot only words it.

const toTaskFlow = "to_task"

func (h *Handler) handleToTaskStart(chatID int64, messageID int, rawEventID string) {
	us := h.store.GetOrCreate(chatID)
	parentID, _, _ := splitInstanceID(rawEventID)
	ev, err := h.api.GetEvent(us.AuthToken, parentID)
	if err != nil || ev == nil || ev.ID == "" {
		h.editOrSend(chatID, messageID, h.t(chatID,
			"❌ Не удалось открыть событие — возможно, оно уже удалено.",
			"❌ Could not open the event — it may already be deleted."),
			keyboards.AgendaActions(h.lang(chatID)))
		return
	}
	us.CurrentFlow = toTaskFlow
	us.FlowStep = "how"
	us.FlowData = map[string]any{"eventID": rawEventID, "title": ev.Title,
		"repeats": ev.Rrule != nil && *ev.Rrule != ""}
	h.editOrSend(chatID, messageID, fmt.Sprintf(h.t(chatID,
		"✅ <b>%s</b> — сделать задачей\n\n🔗 <b>Связать</b> — событие останется и будет указывать на задачу.\n➡️ <b>Перенести</b> — останется только задача.",
		"✅ <b>%s</b> — make it a task\n\n🔗 <b>Link</b> — the event stays and points at the task.\n➡️ <b>Move</b> — only the task remains."),
		format.Escape(ev.Title)), keyboards.ConvertHow(h.lang(chatID), "e2"))
}

func (h *Handler) handleToTaskStep(chatID int64, messageID int, data string) {
	us := h.store.GetOrCreate(chatID)
	if data == "e2x" {
		h.store.ClearFlow(chatID)
		h.editOrSend(chatID, messageID, h.t(chatID, "Отменено.", "Cancelled."), keyboards.AgendaActions(h.lang(chatID)))
		return
	}
	if data == "e2i" {
		h.sendText(chatID, convertExplanation(h.lang(chatID)))
		return
	}
	if us.CurrentFlow != toTaskFlow {
		h.editOrSend(chatID, messageID, h.t(chatID,
			"Это меню устарело — открой событие заново.", "This menu is out of date — open the event again."),
			keyboards.AgendaActions(h.lang(chatID)))
		return
	}
	rawID, _ := us.FlowData["eventID"].(string)
	_, _, isInstance := splitInstanceID(rawID)
	repeats, _ := us.FlowData["repeats"].(bool)

	switch data {
	case "e2m_l", "e2m_m":
		us.FlowData["mode"] = map[string]string{"e2m_l": "link", "e2m_m": "move"}[data]
		if repeats {
			text := h.t(chatID,
				"🔁 Это повторяющееся событие.\n\n<b>Вся серия</b> — задача тоже будет повторяться.",
				"🔁 This event repeats.\n\n<b>Whole series</b> — the task repeats too.")
			if isInstance {
				text += h.t(chatID, "\n<b>Только этот раз</b> — задача на один день, серия событий останется.",
					"\n<b>Just this once</b> — a task for one day; the series of events stays.")
			} else {
				text += h.t(chatID, "\n\n⚠ Открыта вся серия, поэтому один день выбрать нельзя — открой событие из дня календаря.",
					"\n\n⚠ The whole series is open, so one day cannot be chosen — open the event from a calendar day.")
			}
			h.editOrSend(chatID, messageID, text, keyboards.ConvertRepeat(h.lang(chatID), "e2", isInstance))
			return
		}
		h.showToTaskCard(chatID, messageID)
	case "e2r_s", "e2r_o":
		if data == "e2r_o" && !isInstance {
			return // not offered; a crafted button changes nothing
		}
		us.FlowData["repeat"] = map[string]string{"e2r_s": "series", "e2r_o": "once"}[data]
		h.showToTaskCard(chatID, messageID)
	case "e2ok":
		h.finishToTask(chatID, messageID)
	}
}

func (h *Handler) toTaskReq(chatID int64, dry bool) (string, api.ToTaskReq) {
	us := h.store.GetOrCreate(chatID)
	rawID, _ := us.FlowData["eventID"].(string)
	mode, _ := us.FlowData["mode"].(string)
	repeat, _ := us.FlowData["repeat"].(string)
	return rawID, api.ToTaskReq{Mode: mode, Repeat: repeat, DryRun: dry}
}

func (h *Handler) showToTaskCard(chatID int64, messageID int) {
	us := h.store.GetOrCreate(chatID)
	rawID, req := h.toTaskReq(chatID, true)
	res, err := h.api.EventToTask(us.AuthToken, rawID, req)
	if err != nil {
		h.store.ClearFlow(chatID)
		h.editOrSend(chatID, messageID, h.t(chatID, "❌ Не получилось: ", "❌ Did not work: ")+format.Escape(h.errorText(chatID, err)),
			keyboards.AgendaActions(h.lang(chatID)))
		return
	}
	title, _ := us.FlowData["title"].(string)
	h.editOrSend(chatID, messageID, toTaskCard(h.lang(chatID), title, *res, req.Mode, req.Repeat),
		keyboards.ConvertConfirm(h.lang(chatID), "e2"))
}

func (h *Handler) finishToTask(chatID int64, messageID int) {
	us := h.store.GetOrCreate(chatID)
	rawID, req := h.toTaskReq(chatID, false)
	if req.Mode == "" {
		h.store.ClearFlow(chatID)
		return
	}
	res, err := h.api.EventToTask(us.AuthToken, rawID, req)
	h.store.ClearFlow(chatID)
	if err != nil {
		h.editOrSend(chatID, messageID, h.t(chatID, "❌ Не получилось: ", "❌ Did not work: ")+format.Escape(h.errorText(chatID, err)),
			keyboards.AgendaActions(h.lang(chatID)))
		return
	}
	h.handleTaskAction(chatID, messageID, res.Task.ID)
}

// toTaskCard words the dry run. Pure, so every line is tested.
func toTaskCard(lang i18n.Lang, title string, r api.ToTaskResult, mode, repeat string) string {
	var b strings.Builder
	fmt.Fprintf(&b, i18n.T(lang, "✅ <b>%s</b> → задача\n\n", "✅ <b>%s</b> → task\n\n"), format.Escape(title))
	if due, err := time.Parse("2006-01-02", r.Task.DueDate); err == nil {
		fmt.Fprintf(&b, i18n.T(lang, "срок: %s\n", "due: %s\n"), dayLabel(lang, due))
	}
	if r.Task.EstimatedMinutes != nil {
		fmt.Fprintf(&b, i18n.T(lang, "оценка: %s\n", "estimate: %s\n"), format.Duration(*r.Task.EstimatedMinutes))
	}
	b.WriteString(i18n.T(lang, "описание, теги → как есть\n", "description, tags → as they are\n"))
	if r.Task.Rrule != nil {
		b.WriteString(i18n.T(lang, "🔁 повтор → задача повторяется\n", "🔁 repeat → the task repeats\n"))
	}
	for _, code := range r.Lost {
		b.WriteString(lostLine(lang, code) + "\n")
	}
	switch {
	case mode == "link":
		b.WriteString(i18n.T(lang, "\n🔗 Событие останется и будет указывать на задачу.", "\n🔗 The event stays and points at the task."))
	case repeat == "once":
		b.WriteString(i18n.T(lang, "\n➡️ Этот день уйдёт из серии, серия событий останется.", "\n➡️ This day leaves the series; the series stays."))
	default:
		b.WriteString(i18n.T(lang, "\n➡️ Событие удалится, останется задача.", "\n➡️ The event is deleted; the task remains."))
	}
	return b.String()
}

// lostLine words a lost-field code from api-go (events.Lost*).
func lostLine(lang i18n.Lang, code string) string {
	switch code {
	case "start_time":
		return i18n.T(lang, "⚠ час начала → у задачи нет", "⚠ start hour → tasks have none")
	case "color":
		return i18n.T(lang, "⚠ цвет → у задачи нет", "⚠ colour → tasks have none")
	case "location":
		return i18n.T(lang, "⚠ место → у задачи нет", "⚠ place → tasks have none")
	case "reminders":
		return i18n.T(lang, "⚠ напоминания → у задачи будут свои, по умолчанию", "⚠ reminders → the task gets your default ones")
	default:
		return i18n.T(lang, "⚠ ещё кое-что не переносится", "⚠ something else does not carry over")
	}
}
```

⚠ `splitInstanceID` — `handlers/instanceid.go`, сверить возвращаемые значения. `dayLabel` —
`daylabel.go:28`. `keyboards.AgendaActions` — существующая.

**Кнопка на событии.** `keyboards.EventEditor(lang, id, rawID string)`: перед строкой
«💾 Сохранить / 🗑» добавить
`tgbotapi.NewInlineKeyboardRow(tgbotapi.NewInlineKeyboardButtonData(i18n.T(lang, "✅ Сделать задачей", "✅ Make it a task"), "e2t_"+rawID))`.
`EventCard(lang, id)` — та же строка с `id`. В `handleEventCard` передать `eventID` (сырой, как
пришёл) третьим аргументом; остальные вызовы `EventEditor` (`grep -rn "EventEditor(" bot/internal`)
— передать тот id, что у них есть.

⚠ Кнопка на `EventEditor` жмётся посреди черновика (`new_event` / `card`). `handleToTaskStart`
заменяет flow — черновик правок **теряется**, это ожидаемо: пользователь выбрал другое действие.
Одной строкой в тексте шага 1 это не говорится — достаточно, что «❌ Отмена» возвращает к событиям.

**Роутинг** (`handler.go`), раньше любого `eve`/`evd`:

```go
case strings.HasPrefix(data, "e2t_"):
	h.handleToTaskStart(chatID, cb.Message.MessageID, strings.TrimPrefix(data, "e2t_"))
case strings.HasPrefix(data, "e2"):
	h.handleToTaskStep(chatID, cb.Message.MessageID, data)
```

⚠ `grep -rn '"e2' bot/internal/keyboards` — ни один существующий префикс не должен начинаться с `e2`.
`schedule_test.go`: `routed` += `"e2t_", "e2"`; `scheduleKeyboards()` += `"ConvertHowE2": keyboards.ConvertHow(i18n.RU, "e2")`,
`"ConvertRepeatE2": keyboards.ConvertRepeat(i18n.RU, "e2", true)`, `"EventCard": keyboards.EventCard(i18n.RU, uuid)`.
⚠ `EventEditor` содержит `dr_*`-кнопки, которых в `routed` нет, — **не** добавлять его в этот
тест целиком; вместо этого отдельный тест: в `EventEditor(i18n.RU, "e1", "e1:2026-10-22")` есть кнопка
с данными `e2t_e1:2026-10-22`.

- [ ] **Step 4:** весь модуль → ноль `FAIL`.
- [ ] **Step 5: Саботаж — по одному:**
  1. в `showToTaskCard` `true` → `false` в `toTaskReq` → `TestToTaskShowsTheDryRunBeforeWriting` `FAIL`;
  2. в `ConvertRepeat(…, isInstance)` передать `true` → `TestOnceIsOfferedOnlyForAnOccurrence` `FAIL`;
  3. в `lostLine` убрать `case "location"` → `TestEveryLostCodeHasWords` `FAIL`.
- [ ] **Step 6: Commit** — `feat(bot): event → task — from the event card, with the API's own card of what is lost`

---

### Task 6: коды ошибок A1 человеческим языком

**Files:** Modify `bot/internal/handlers/errors.go`; test в существующем `errors_test.go` (если его нет — `bot/internal/handlers/errors_a1_test.go`).

- [ ] **Step 1: Падающий тест**

```go
// Every code the A1 endpoints answer with has a sentence, not a fallback.
func TestA1ErrorCodesHaveWords(t *testing.T) {
	h, _, _ := convertAPI(t, "", `{}`)
	fallback := h.errorText(920, &api.Error{Status: 400, Code: "SOMETHING_UNKNOWN"})
	for _, code := range []string{"REPEAT_CHOICE_REQUIRED", "OCCURRENCE_REQUIRED", "REPEAT_UNSUPPORTED", "NEEDS_TIME"} {
		if got := h.errorText(920, &api.Error{Status: 400, Code: code}); got == fallback {
			t.Errorf("%s falls back to the generic text", code)
		}
	}
}
```

- [ ] **Step 2:** прогнать → `FAIL` на всех четырёх.
- [ ] **Step 3:** в `errors.go` рядом с `case "NOT_AN_OCCURRENCE":`:

```go
case "REPEAT_CHOICE_REQUIRED":
	return h.t(chatID, "Это повторяется — выбери «вся серия» или «только этот раз».",
		"This repeats — choose «whole series» or «just this once».")
case "OCCURRENCE_REQUIRED":
	return h.t(chatID, "Не знаю, какой именно день. Открой событие из дня календаря.",
		"I don't know which day. Open the event from a calendar day.")
case "REPEAT_UNSUPPORTED":
	return h.t(chatID, "Такой повтор перенести не получится — его правило старое. Выбери «только этот раз».",
		"This repeat cannot be carried over — its rule is an old one. Choose «just this once».")
case "NEEDS_TIME":
	return h.t(chatID, "Для календаря нужно время начала.", "The calendar needs a start time.")
```

(Сверить форму: в `errors.go` ветки возвращают строку или присваивают — повторить соседнюю.)

- [ ] **Step 4:** весь модуль → ноль `FAIL`. **Саботаж:** удалить `case "NEEDS_TIME"` → `FAIL`; откатить.
- [ ] **Step 5: Commit** — `feat(bot): the task↔event errors in words`

---

### Task 7: проверка и выкат на dev

- [ ] **Step 1:** `cd bot && gofmt -l $(git diff --name-only develop~6 -- 'bot/*.go' | sed 's#bot/##')` → пусто;
  `go build ./... && go vet ./... && go test -count=1 ./... 2>&1 | grep -E '^(--- FAIL|FAIL)'` → пусто;
  `api-go` — не менялся, но прогнать (`DATABASE_URL=…`) → ноль `FAIL`.
- [ ] **Step 2:** 🔴 **Выкат только после слова Дениса про push.** Порядок:
  1. `git push origin develop` → дождаться CI (`gh run watch`), dev API с A1 поднят;
     проверить `curl -s -o /dev/null -w '%{http_code}' -X POST https://dev.neuroboost.website/api/events/x/to-task` → `401` (роут есть), не `404`.
  2. dev-бот: `cd /opt/neuroboost-bot && mv src src.bak-2026-09-2x`, затем
     `git archive --prefix=src/ develop:bot | ssh -i ~/.ssh/ufo_servers root@185.214.10.107 'cd /opt/neuroboost-bot && tar x'`,
     `docker compose up -d --build bot`; `ls src/internal/handlers/toevent.go` на хосте — **файл есть**.
  3. Логи — только через `sed`-фильтр токена (CLAUDE.md gotcha 14).
- [ ] **Step 3:** чеклист для Дениса — строки A3 спеки, в `docs/razbor-prohoda-v04114-2026-09-21.md`
  §«Что перепроверить руками» дописать раздел «задача ↔ событие» по одной строке на пункт A3.
- [ ] **Step 4: Commit** — `docs: what to press to check task↔event on dev`

---

## Покрытие спеки §A2/§A3 → задачи

| Требование | Задача |
|---|---|
| `[⏰ Запланировать]` и `[📅 В календарь]` на карточке задачи | 3 (кнопки), 4 (путь) |
| «📅 запланирована на вт 15:00» + кнопка открыть событие | 3 |
| `[✅ Сделать задачей]` на карточке события | 5 |
| Как? связать/перенести с одной строкой «что будет» | 4, 5 |
| Если повтор: вся серия / только этот раз | 4, 5 (для события — только у вхождения, решение 1) |
| Недостающее: время (слоты + своё), длительность без оценки | 4 |
| День, если у задачи нет срока | 4 — входит в слот / текст (решение 2) |
| Карточка «что чем станет», потери ⚠ | 4 (из задачи), 5 (из `dry_run`) |
| `[✅ Готово]` `[ℹ️ Что это?]` `[❌ Отмена]` | 4, 5 |
| Список: связанные с «📅 вт 15:00» | 3 |
| Бот показывает `TODO` и `SCHEDULED` | 2 |
| A3: быстрый «⏰» — задача осталась в списке | 2 (+ A1 Task 3) |
| A3: «Готово» у связанной закрывает задачу; прошедшее событие — нет | ничего не меняется: `task_done_` уже закрывает задачу; событие статус задачи не трогает (A1) — проверяется руками по чеклисту Task 7 |
